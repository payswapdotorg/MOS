# MarketingOS Implementation State — v1.4

**Repository:** `payswapdotorg/MOS`
**Purpose:** successor Tech Lead coordination ledger.

This is not an architectural authority. Actual Git history, PRs, code and verification evidence outrank stale status here.

## Current state

| Work Item | State | Notes |
|---|---|---|
| MKT-001..MKT-012 | ACCEPTED/MERGED | Foundation through sandbox/runtime lifecycle accepted before the v1.4 continuation. |
| MKT-009 correction | ACCEPTED/MERGED | Workflow-instance history consistency backstop accepted and merged. |
| MKT-013 | ACCEPTED/MERGED | Reconciled: no prior PR existed in this repository. Dispatched fresh from the frozen contract (worker session mkt-013, GLM-5.3 agents tab, Full-Stack). PR #1 squash-merged at ee246d1d3596 after full Tech Lead verification: lint 0, tsc 0, arch:check 0 violations, unit 292/292, architecture 115/115, integration 308/308 vs real PostgreSQL. |
| MKT-014 | ACCEPTED/MERGED | First git-channel worker delivery (worker session mkt-014, GLM-5.3 agents tab, Full-Stack): branch mkt/014-worker-delivery pushed directly by the worker (4cfb756 on af5cce9) after chat-transit corruption made report delivery unreliable. PR #2 squash-merged at 36d60c0e after independent Tech Lead verification: lint 0, tsc 0, arch:check 0 violations (214 files), unit 301/301, architecture 124/124, integration 321/321 vs real PostgreSQL. Worker-reported gate matched the re-run exactly. |
| MKT-017 | ACCEPTED/MERGED | Git-channel worker delivery (worker session mkt-017, GLM-5.3 agents tab, Full-Stack): branch mkt/017-worker-delivery pushed by the worker (68118ad on base 68d8c8a0, pre-013/014 main); Tech Lead rebased onto current main (shared-file registrations combined with evidence+metrics; migration list 015/016/018) and independently verified: lint 0, tsc 0, arch:check 0 violations (220 files), unit 326/326, architecture 133/133, integration 337/337 vs real PostgreSQL. PR #3 squash-merged at 26f7665e. |
| MKT-018 | ACCEPTED/MERGED | Git-channel worker delivery on current main (base 63205c2): branch mkt/018-worker-delivery (f065440), no shared-file conflicts. PR #4 squash-merged at 2ebcd57 after independent Tech Lead verification: lint 0, tsc 0, arch:check 0 violations (229 files), unit 371/371, architecture 145/145, integration 354/354 vs real PostgreSQL. Worker completed the full implementation server-side while its browser tab was submit-dead (fresh tab reconnected to the live session — recovery documented). Honest disclosures verified in code. |
| MKT-025 | ACCEPTED/MERGED | Git-channel worker delivery (worker session mkt-025, GLM-5.3 agents tab, Full-Stack): branch pushed after two sandbox destructions (files rebuilt from session context); Tech Lead rebased onto current main (shared registrations + migration list 015/016/017/018/020; five small integration fixes) and independently verified: lint 0, tsc 0, arch:check 0 violations (236 files), unit 392/392, architecture 155/155, integration 376/376 vs real PostgreSQL. PR #5 squash-merged at 88dcbbc. |
| MKT-026 | ACCEPTED/MERGED | Git-channel worker delivery on current main (base c4edcf2): branch mkt/026-worker-delivery (671ddeb) pushed by the worker at 17:50Z; PR #6 squash-merged at 2c6af75 after independent Tech Lead verification: lint 0, tsc 0, arch:check 0 violations (246 files), unit 422/422, architecture 177/177, integration 411/411 vs real embedded PostgreSQL. Migration 023_jobs.sql. |
| MKT-020 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-020b): branch mkt/020-worker-delivery (8287b38, base ce89c06 — pre-025 main). Tech Lead merged onto current main (three-module coexistence in shared registration files resolved: field-agents + jobs + agents; migration list 022<023; one wiring assertion relaxed to match merged sibling registrations, intent preserved) and verified on the merged tree: lint 0, tsc 0, arch:check 0 violations, unit 434/434, architecture 187/187, integration 424/424 vs real embedded PostgreSQL (two consecutive clean runs; one transient env-contamination failure investigated and ruled out). PR #7 squash-merged at 308bbaa. Migration 022_logical_agents.sql. FOLLOW-UP FIX: the PR accidentally tracked a node_modules symlink (git worktree artifact; .gitignore's dir-only pattern missed the symlink form) — broke local node_modules and CI once; removed + .gitignore hardened at fa9352a, CI green, local gates re-verified (integration 424/424). |
| MKT-027 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-027, GLM-5.3 agents tab, Full-Stack): branch mkt/027-worker-delivery (6ebd8d2, base be8505b). Worker survived a mid-run sandbox destruction (workspace torn down during integration tests) and REBUILT the full implementation from session context after a Tech Lead recovery nudge; initial push failed on credentials (plain clone), corrected via token-URL push. Migration numbering disclosure honored: 024_field_execution.sql (021 sorts before 023_jobs.sql which creates the FK-referenced jobs table). Tech Lead merged onto current main (with MKT-020 logical agents; shared registration files resolved) and verified: lint 0, tsc 0, arch:check 0 violations, unit 457/457, architecture 209/209, integration 459/459 vs real embedded PostgreSQL. PR #8 squash-merged at c0dcb2c. |
| MKT-021 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-021, GLM-5.3 agents tab, Full-Stack, chat 16b742c0): branch mkt/021-worker-delivery (7229ab6, base 3acb711) — the worker kept implementing server-side through the personal-usage freeze while its tab looked stalled (branch pushed 05:04Z during the freeze; lesson 19b). Migration 025_policies.sql (pre-reserved). Tech Lead merged onto current main (4 shared-file conflicts resolved: application/routes/composition-root — policies + jobs-visits wiring coexist; infra-adapters expected migration list reordered 024<025 lexicographic) and verified on the merged tree: lint 0, tsc 0, arch:check 0 violations (259 files), unit 476/476, architecture 233/233, integration 478/478 vs real embedded PostgreSQL. PR #9 squash-merged at b09c871ca1. |
| MKT-022 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-022e, GLM-5.3 agents tab, Full-Stack, chat e5448443 — the real conversation id; the create's recorded URL was a pre-send shell, lesson 2026-09-11): branch mkt/022-worker-delivery (78cac5c, base 0e1485d = exact main). Station verification: lint 0, tsc 0, arch:check 0 violations (26 frozen modules, 272 files), unit 511/511, architecture 259/259, integration 501/501 vs real embedded PostgreSQL. 13 files, +7055 lines: extensions module (public contract, registration/versioning/compatibility/permissions/lifecycle), migration 028_extensions.sql, 4 test suites. PR #10 squash-merged at 3bb7874. |


| MKT-035 | ACCEPTED/MERGED | RECONCILE resolved: no separate implementation required. The work-item matrix (spec/work-item-matrix-v1.3.md) already credits HUMAN-001/HUMAN-AC-01..03 to MKT-025 (PR #5) and MKT-026 (PR #6) — the MKT-025 dispatch implemented the GENERIC Human Agent authority (/field-agents header: "the GENERIC HUMAN AGENT authority... a second human-execution module is forbidden"; human_agents table, 8-specialization frozen registry as capability tags, platform identity, multi-Agency participation via /agencies membership). Objective re-verification on main f1ab1af (2026-09-11, Tech Lead station): lint 0, tsc 0, arch:check 0, unit 457/457, architecture 209/209, integration 459/459 vs real embedded PostgreSQL; explicit HUMAN-AC coverage verified: AC-01 (field-agents-api.test.ts: full profile field groups + platform identity without agency), AC-02 (unit: frozen 8-specialization registry, tags on one profile; architecture: /field-agents exports no job/execution engine — one Job/Task/Execution authority in /jobs), AC-03 (field-agents-security.test.ts + jobs-isolation.test.ts: no client-data routes, fail-closed boundaries, multi-agency uniform 404). |
| MKT-031 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-031d, chat a7e217d1): branch mkt/031-worker-delivery (c98d201, base 0e1485d). The turn died mid-report (dead-stream disease); work survived committed in-sandbox — recovered via the refresh-icon dangling-turn clear + short token re-issue (n 20→22), worker pushed immediately. Thin work-queue API over the SAME /jobs + /field-agents authorities (no new module, no queue engine): MY QUEUE + discovery + offer accept/decline converging with the MKT-026 surface. Tech Lead merged main (MKT-022 extensions) into the delivery — one header-comment conflict, both registrations coexist — and verified: lint 0, tsc 0, arch:check 0 violations (276 files), unit 518/518, architecture 266/266, integration 513/513 vs real embedded PostgreSQL. PR #11 squash-merged at e63332f. |
| MKT-015 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-015k, chat 88eb965c): branch mkt/015-worker-delivery (13bcb3a, base 0e1485d) — the turn died mid-report (dead-stream disease); work survived committed in-sandbox at 8ced94c; recovered via refresh-icon dangling-turn clear + short token re-issue (n 4→6), worker re-verified gates and pushed. Experiment model: lifecycle state machine, frozen causal-evidence taxonomy, set-once decisions, uncertainty/conclusion semantics, migration 019_experiments.sql. Tech Lead merged main into the delivery (composition-root modules-list conflict: experiments + extensions coexist) and verified: lint 0, tsc 0, arch:check 0 violations (282 files), unit 538/538, architecture 274/274, integration 534/534 vs real embedded PostgreSQL. PR #12 squash-merged at fbc9941. |
| MKT-016 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-016, chat a48a24e9): branch mkt/016-worker-delivery (db6cc4e, base fbc9941 = exact main at dispatch, push-early rule honored). Learning model (LEARN-001): validation semantics, append-only learning history, experiment linkage, migration 027_learnings. Station verification: lint 0, tsc 0, arch:check 0 violations, unit 555/555, architecture 282/282, integration 554/554 vs real embedded PostgreSQL. PR #13 squash-merged at 781823d. |
| MKT-036 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-036b, chat fd17b120): branch mkt/036-worker-delivery (1114f3e, base fbc9941, push-early rule honored). Versioned domain pack framework (PACK-001): immutable-once-published semantics, migration 030_domain_packs. Tech Lead merged main (MKT-016 learnings) into the delivery (composition-root modules-list conflict: learnings + domainPacks coexist) and verified: lint 0, tsc 0, arch:check 0 violations, unit 575/575, architecture 303/303, integration 574/574 (first run 573/574 with one transient concurrency flake — re-run clean, ruled out per MKT-020 precedent). PR #14 squash-merged at 3a989fa. |
| MKT-030 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-030, chat a5fc1a0d): branch mkt/030-worker-delivery (214cb60, base 3a989fa, push-early rule honored). Client Decision Room (UI-001) as a client-scoped route family over the EXISTING /reporting authority — pure live aggregation, read-only by construction. Station verification: lint 0, tsc 0, arch:check 0 violations, unit 587/587, architecture 311/311, integration 587/587 vs real embedded PostgreSQL. PR #15 squash-merged at 6f89be7. |
| MKT-023 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-023, chat 868a806a — recovered mid-flight: dead turn cleared via refresh-icon, continuation+token re-send, worker re-engaged and pushed): branch mkt/023-worker-delivery (2b149de, base 0e1485d stale — pre-015/016/031/036 merges). Provider integration boundary (INT-001): generic ports, first-party adapter registry as injected data, connection/capability metadata, policy-gated provider execution, append-only webhook/event ledger, migration 029_integrations. Tech Lead merged main into the delivery (4 shared-file conflicts resolved: additive comment/registration blocks kept both sides; duplicate modules-list merged; expected-migration list reordered 029 after 028 lexicographic) and verified: lint 0, tsc 0, arch:check 0 violations, unit 610/610, architecture 311/311, integration 619/619 vs real embedded PostgreSQL. PR #16 squash-merged at ca529ea. |
| MKT-024 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-024b, chat c1f2b019, GLM-5.3 agents tab, Full-Stack — recovered post-sandbox-reset: worker had completed + committed server-side, push waited on a token; token re-issue nudge 06:14Z, branch pushed 06:40Z): branch mkt/024-worker-delivery (4aac27d, base c6f79ec = exact main). Five first-party connectors (Meta, Google Ads, generic analytics, CRM, commerce/CMS) as concrete adapters under /integrations internal/adapters/** on the platform HttpCallPort (zero provider SDKs in src/ — station-verified), injected as DATA at the composition root; arch-check CONCRETE_ADAPTER_ACCESS sanctioned exception (composition-root-only, frozen-matrix provision documented); two real bug fixes found by the new acceptance tests (NormalizedRateLimit null-field serialization vs migration 029 CHECK; requireConnectionInClient policy added to connect/suspend/read/mutate/webhook routes); observation-delivery API + 3 new suites (connectors E2E, boundary, unit). Station verification: lint 0, tsc 0, arch:check 0 violations (318 files), unit 621/621, architecture 325/325, integration 634/634 vs real embedded PostgreSQL (worker-reported 9 MinIO failures environmental — do not reproduce at station with pinned binary). PR #18 squash-merged at aeaa1f7. |
| MKT-028 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-028, chat 2d1d0d47, GLM-5.3 agents tab, Full-Stack; recovered post-sandbox-reset via token re-issue nudge — sandbox wiped, worker REBUILT the full delivery from session context and pushed 0a4e41a): acquisition-pilot composition service OUTSIDE src/modules (MKT-011 pooled-runtime precedent) — contract/template/pilot-flow compositing ONLY frozen public contracts (zero module-internal imports, station-verified); pilot owns NO durable state; 3 suites (unit 15, architecture 11, E2E 8 incl. golden path + authority-bypass negatives). Stale base c6f79ec (pre-MKT-024) — Tech Lead merged main into the delivery (auto-merge clean, zero true conflicts) and verified the MERGED tree at 4665c4d: lint 0, tsc 0, arch:check 0 violations (324 files), unit 636/636, architecture 336/336, integration 642/642 vs real embedded PostgreSQL. PR #19 squash-merged at dc29b60. |
| MKT-037 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-037b, chat a3b83868, GLM-5.3 agents tab, Full-Stack): the session survived TWO sandbox destructions (orchestration tooling bug — retry-suffixed session names did not match sandbox rows; dispatch_worker.py patched 2026-09-12 with bare mkt-NNN prefix keywords) and honored the push-early rule — branch 958e438 pushed BEFORE the completion report streamed. Creator Operations Domain Pack under /domain-packs internal/packs/creator-operations (contract/guards/manifest/module/store + API routes + migration 031_creator_operations 1027 lines, frozen-list appended): exactly 8 pack-owned tables on existing agency/client scope chains (no second tenant authority), NO pack-owned observation tables (AC-02 routes observations to common /evidence + /metrics), TaskProfile/AI-Router only (AC-03), generic Human Agent model (AC-04), zero provider SDKs (AC-05), AC-06 side-effect provenance structurally required; §21 material-key validators, append-only/immutability triggers, frozen-lifecycle backstops. Stale base c6f79ec — main merged in (auto-merge clean); MERGED tree verified: lint 0, tsc 0, arch:check 0 violations, unit 654/654, architecture 353/353, integration 662/662 vs real embedded PostgreSQL. PR #20 squash-merged at d0f95e0. |
| MKT-032 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-032b, chat 948e2b6a, GLM-5.3 agents tab, Full-Stack — second attempt after a capacity-queue shell rollback): Extension Developer Portal as a thin route family over the SAME /extensions authority + delegated /policies permission approvals — publication (frozen platform_developer role), catalog/version history/compatibility, testing hook (thin pass-through, no second engine), permission review (delegated declarations; surface never evaluates permissions), workspace install/configure/authorize/disable/uninstall, version management (structural pinning + disclosed non-atomic upgrades). NO migration (pure surface). E2E 1218 lines with authority-state assertions at every lifecycle step; unapproved-install fails AT THE AUTHORITY; cross-client uniform 404; forged field/transition rejections. Stale base f046582 — main merged in (ONE routes.ts additive conflict resolved keeping both families). Merged tree 18d5469: lint 0, tsc 0, arch:check 0, unit 654/654, architecture 361/361, integration 682/682 vs real embedded PostgreSQL. PR #21 squash-merged. |
| MKT-033 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-033d, chat 65a64d8a, GLM-5.3 agents tab, Full-Stack — third attempt after two capacity-queue rollbacks; deleted the dead 032b chat to free the slot). Deployment/runtime productionization on exact base bbd3a30: EXPLICIT-CONFIG contract (config.ts AI Runtime wiring complete-or-absent like S3 + describeConfig() redacted auditable startup report; negative tests abort on invalid/ambiguous config, no silent fallbacks), composition-root-only OpenRouterAdapter behind the ProviderAdapter contract (no SDK, rides HttpCallPort), startup migration VERIFICATION (applies missing, hard-fails on checksum drift), graceful-shutdown drain now tested. DEPLOY-AC-01 → tests/integration/deployment-topology.test.ts (control plane + worker boot from the SAME explicit config vs real embedded PG; health + config.effective assertions); DEPLOY-AC-02 → tests/integration/queue-recovery-no-redis.test.ts (SIGKILL mid-queue with Redis ABSENT ENTIRELY; authoritative PostgreSQL recovery — within-window never blindly replayed, post-window reclaim with at-least-once attempts 1→2, idempotent resubmission converges). NO migration (persistence beyond existing tables not required; disclosed). Station verification: lint 0 / tsc 0 / arch:check 0 violations (338 files, 26 frozen modules) / unit 659/659 (+5) / architecture 361/361 / integration 698/698 (+16) vs real embedded PostgreSQL. PR #22 squash-merged at 20a112c. Runbook: docs/implementation/MKT-033.md (361 lines). |
| MKT-038 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-038d, chat 01b298fb, GLM-5.3 agents tab, Full-Stack — fourth attempt after a voided false-dead original (which was mid-gates at void time), usage-limit rollbacks, and capacity queues; the deleted original 038 chat's unpushed work was rebuilt fresh by 038d). Creator-platform first-party connector (588 lines) under /integrations internal/adapters/creator-platform on the platform HttpCallPort (zero provider SDKs — station-verified): seven normalized creator capabilities mapped one-to-one onto the MKT-037 pack §6 integration-bindings (provider-neutral labels in the pack, provider shapes only in the adapter); fail-closed policy-gated mutations (the CREATOR-AC-06 provider halves); loopback sandbox provider (tests/integration/helpers/sandbox-provider.ts) serving fixture payloads over REAL HTTP for the E2E. E2E 1292 lines: E2E-AC-02 golden path (provider event → subjects → Goal → Workflow via pack template → AI Router task → Human Agent Job → APPROVED send through the adapter → Evidence/Outcome → experiment → Learning) + fail-closed client-scoped deny before any provider traffic + tenant isolation uniform 404s + forged-webhook-signature rejection with nothing recorded + replay semantics (fresh immutable rows). MKT-023 registry test updated to expect the six connectors (additive DATA evolution as the frozen boundary documents). Station verification on exact base 8870a49: lint 0 / tsc 0 / arch:check 0 violations (340 files) / unit 659/659 / architecture 365/365 (+4) / integration 703/703 (+5) vs real embedded PostgreSQL. PR #23 squash-merged at 4b84c9a. |
| MKT-034 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-034, chat 00549d91, GLM-5.3 agents tab, Full-Stack, dispatched clean into the post-peak capacity window after zombie cleanup). End-to-end acquisition operating loop as a workers-area composition service (src/workers/acquisition-loop/ — the MKT-028 acquisition-pilot precedent, one hop wider): contract.ts (246 lines types/constants only), loop-flow.ts (812 — the wiring service), template.ts (667 — the pure template package). ONE workflow instance carries all three execution legs: AI path (AI Runtime routing: TaskProfile → selection + cascadeRun), human field path (§18 Job/Visit/Outcome with during-execution evidence capture, server-derived scope, field-agent provenance), extension path (MKT-032 portal chain publish → permission-review → install → configure → authorize → invoke, policy-gated invocation, content-addressed artifact verified by sha256). Shared Goal/Workflow/Evidence lifecycle: per-leg evidence rows + outcome source_fact all on the loop client; metrics with evidence provenance; §16 experiment conclusion citing same-Client evidence refs (append-only transition history verified verbatim: mark_ready/start/begin_analysis/conclude); §17 Learning citing evidence + the concluded experiment; Client Decision Room read surfaces the whole loop (goals with success-criteria metric, workflow instanceCounts, why/learnings, evidenceQuality 3×C + 1×B). Negative tests: foreign-agency install denied AT THE AUTHORITY, forged transitions 409, invocation input guards fail-closed, tenant isolation uniform 404, foreign TaskProfile routing NotFoundError, loop fences (cross-Client citation 404 leaves experiment running; live cap blocks second start; Learning reference fences). PURELY ADDITIVE: 6 files +4128 lines, zero existing files touched, no migration (proof over existing persistence — disclosed). Station verification on exact base 218afdb: lint 0 / tsc 0 / arch:check 0 violations (346 files, 26 frozen modules) / unit 676/676 (+17) / architecture 376/376 (+11) / integration 710/710 (+7) vs real embedded PostgreSQL. Worker-disclosed MinIO/S3 failures environmental (upstream 410 Gone, fails identically on clean main) — station passes 710/710 with the pinned .test-deps binary. PR #24 squash-merged at 35b6a42. |
| MKT-039 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-039, chat 6d87f0c2, GLM-5.3 agents tab, Full-Stack — landed at 21:55Z after a ~3h launch gate closed since ~18:30Z, cracked by the 41-stale-agent-chat purge + persistent verified assault; the gate epoch cost 29+ phantom sends, all caught by server-truth verification). Creator Operations end-to-end experience proof (E2E-AC-02 + browser/API authorization) as PURELY ADDITIVE integration suites (3 files, +3254 lines, zero src changes, no migration): creator-operations-experience-e2e.test.ts (1854) — the golden path as ONE test: signed provider webhook → MKT-023 ledger + source_fact evidence → pack subjects + inbound observation → ACTIVE workspace-scoped Goal → pack conversation-triage template published/installed/materialized (definition ACTIVE + instance RUNNING) → TaskProfile provisioning → /executions AI attempt (taskLink workflow-node) → AI Router real cascade (cheap-first, validated draft) → execution lifecycle to succeeded → Human Agent Job THROUGH THE GENERIC WORK QUEUE (chatter projection via /workflows instances/jobs, §18 offer, queue DISCOVERY with descriptor-view forbidden-key scan, MY QUEUE obligation display, QUEUE ACCEPT = the same modules.jobs.acceptOffer with direct-surface replay convergence replayed=true + byte-identical views, outcome through the DIRECT /jobs surface citing send evidence) → the CREATOR-AC-06 approval chain (fail-closed unapproved send 403 with ZERO rows + ZERO provider traffic, reviewer approval, gated send born 'sent' carrying the allowing policyDecisionId) → provider send through /integrations mutate (executeMutation → CreatorPlatformAdapter → REAL HTTP sandbox send, exactly one provider side effect) → pack §7 observation mapping into COMMON /evidence + /metrics → integration read-sync (readAccountMetrics → 3 METRIC-001 envelopes) → §16 experiment (mark_ready/start/begin_analysis/conclude; the CONCLUSION = the Client Decision citing same-Client evidence) → §17 Learning (evidenceRefs + concluded experimentRef) → instance terminal → CLIENT DECISION ROOM surfaces the WHOLE loop (goals + workflows instanceCounts + why/learnings + evidenceQuality 4×C source_fact + 2×C observation + experiments with resultingDecision VERBATIM + recommendations with no invented lift vocabulary + approvals empty) → shared-lifecycle RE-READ asserted (same workflowInstanceId across execution taskLink/job scope/room recap; provider saw exactly ONE approved send); + 8 authority-fence tests (forged transition + CAS race 409s; foreign TaskProfile 404 + terminal execution 409; queue no-profile 403/foreign offer 404/losing claim 409; provider hop cross-client uniform 404; evidence material-key 422 zero rows; learning hop 409/422 fences; decision-room foreign 404 + TaskProfile foreign-workspace block; pack role enforcement client_collaborator mutate 403 reads allowed). creator-operations-authorization.test.ts (580) — workspace scoping (foreign 404, second same-client workspace reachable), role enforcement (mutations owner|admin, operator/collaborator 403, foreign agency 404 on reads), cross-client indistinguishable 404, pack guards (material/authority-shaped keys 422 zero rows). creator-operations-decision-room-browser.test.ts (820) — the browser half per the MKT-030/031 precedent over real HTTP: UI-AC-01 approver sees evidence/experiment awaiting decision; UI-AC-02 room read-only by construction (405s, authority-shaped probes byte-identical, durable rows unchanged); Client Decision through frozen /experiments authority only (client-side approver 403); approver re-sees recorded decision verbatim + learning + terminal instance. Station verification on exact base aec1d99: lint 0 / tsc 0 / arch:check 0 violations (349 files, 26 frozen modules) / unit 676/676 / architecture 376/376 / integration 727/727 (+17) vs real embedded PostgreSQL. PR #25 squash-merged at f98f593. |
| MKT-040 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-040, chat 69bf6e1d, GLM-5.3 agents tab, Full-Stack — landed first-attempt at ~22:00Z in the open-gate epoch; delivered 621d84c on base aec1d99 (pre-039-merge), rebased clean by the station onto 5893799 — disjoint files). The FINAL convergence Work Item: the authoritative Deployment control plane (DEPLOY-002, DEPLOY-AC-03..09 all seven). 14 files +7970/-12: src/modules/deployments/public.ts (899-line contract: the frozen 9-status lifecycle machine + DEPLOYMENT_TRANSITIONS table + isLegalDeploymentTransition predicate; the closed 4-class runtime vocabulary; identity/provenance/check-report/event record types; the NINE structural ports — workspace ownership, playbook/workflow version snapshots, pack installs, extension/integration availability, policy gate, credential references, execution REQUEST — the MKT-023 structural-port precedent extended to the whole dependency surface with a LITERAL ZERO cross-module import), internal/{module 1? (store 680 + resolution 767 + module)} (resolution service + append-only event ledger + CAS-versioned mutations), migration 034_deployments.sql (744 lines: deployments + deployment_events ONLY; CHECK-fenced lifecycle + closed runtimeClass shape with infrastructure-identity denylist; transition trigger encoding EXACTLY the frozen table; selection-change fence permitting mutation ONLY on redeploy/rollback completion edges; ledger UPDATE/DELETE rejected), API wiring (13 frozen routes, no delete/retry/dispatch surface, DTO authority-field rejection), runbook docs/implementation/MKT-040.md. Tests: deployments-boundary.test.ts (459 static — zero cross-module import incl. type-only, no workflow/execution mutation method in the public API, no second-engine surface, migration creates ONLY the two tables, identity column set complete, frozen trigger table, no infrastructure identity, exactly-13 routes, composition-root structural-port wiring), deployments-api.test.ts (1700 ROUTE+DB: AC-03 full identity persisted + authority-field smuggling 422; AC-04 golden validate→activate with every green check recorded + SIX fail-closed gate negatives — disabled pack, unauthorized extension, suspended integration, disabled credential, policy DENY 403, retired playbook refused at validation; AC-05 invalid transitions rejected + terminal reject-all + idempotent material mutations (duplicate keys converge zero new rows) + concurrent validation CAS exactly-one-wins; AC-06 redeploy+rollback change FUTURE selection with history IMMUTABLE (DB-asserted); AC-08 foreign deployment uniform 404 ≡ unknown + cross-client binding rejected before any write; AC-09 all four runtime classes declarable/persistable riding execution requests; pause/resume round-trip re-runs the full gate on every entry into ACTIVE; workspace listing + member read posture), deployments.test.ts (794 unit: the frozen transition table + guard predicate + regression matrix). infra-adapters list append additive-only with MKT-040 credit. Station verification on rebased main: lint 0 / tsc 0 / arch:check 0 violations (356 files, 26 frozen modules) / unit 689/689 (+13) / architecture 389/389 (+13) / integration 746/746 (+19) vs real embedded PostgreSQL — one environmental SIGTERM race (subprocess exit timing under peak suite load) re-run clean 746/746 + isolated 13/13. PR #26 squash-merged at 50c6146. |
| MKT-019 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-019, chat f169023f, GLM-5.3 agents tab, Full-Stack — landed 22:12Z first-attempt in the open-gate epoch; delivered a3e44f2 on base aec1d99; station rebased onto main with ONE textual conflict in tests/architecture/infra-adapters.test.ts — the frozen expected-migration list, both MKT-019's 032 and MKT-040's 034 entries preserved in numeric order by the station — rebased commit 4b71453 pushed as mkt/019-worker-delivery-rebased, the worker's original branch never rewritten). The AI evaluation framework (AI-003, AI-AC-08 + evaluator regression matrix): 16 files +6800/-70 — src/modules/ai-runtime extended (the reserved slice, NOT a new module): public.ts evaluation contract (registerEvaluator with ACTIVE-key fence + retire→re-register-as-new-identity; evaluateTask deriving the request from the TaskProfile evaluator contract; requestReview human-review hook; EVALUATION_FORBIDDEN_INPUT_KEYS = server-derived authority fields + evaluator-selection + outcome fields + business-outcome-shaped keys (metricId/kpiId/experimentId/lift/...) — the AI-AC-08 machine-checked separation at every entry), internal/ai-evaluation-store.ts + internal/evaluation/evaluate.ts (the orchestration: multi-evaluator runs record every contract evaluator in order; §12 outcome records verdict/score(0-1)/dimensions/evidenceRefs/uncertaintyOrLimitations execution- and usage-linked with server-side provenance — the usage-telemetry evaluationRef wiring lands here; the ENGINE-supplied model-judge path records verbatim as advisory evidence, no engine = ConflictError never a silent half-evaluation), migration 032_ai_evaluations.sql (ai_evaluators + ai_evaluations + ai_review_requests + ai_review_transitions: CHECK-fenced kinds/verdicts/scores, append-only triggers, terminal lifecycles, scope-chain backstops, content-immutable evaluator registry). Tests: ai-evaluation-boundary.test.ts (static AI-AC-08 proof: actual import-graph scan of all ai-runtime files against metrics/experiments/reporting; migration-column scan for business-outcome columns; input-key guard scan; provider-neutrality scan; single-authority scan; hook-not-engine — zero /jobs or /field-agents imports), ai-evaluation.test.ts (16 integration tests vs real PG: registration is platform-admin territory 403; ACTIVE-key duplicate 409; TaskProfile-derived outcomes; 422 negative key rejection incl. credentials; unresolvable evaluator key uniform 404; §8-style replay convergence 200-same-rows vs 409 different-payload; foreign execution 404; usage-telemetry link + foreign usage 404 + telemetry evaluationRef must be REAL; evidence citations validated through /evidence (fabricated/foreign 404); review hook pending→approved/rejected/dismissed with append-only transitions + second decision terminal 409 + foreign review 404 + decision-keys 422; DB append-only rewrite rejection under direct SQL; cross-workspace scope-chain trigger rejection; review decision EXACTLY-ONE under concurrency), ai-evaluation-regression-matrix.test.ts (565-line frozen scenario suite: G1 multi-evaluator in order, G2 empty contract no outcomes, every built-in kind, every frozen verdict), unit ai-evaluation.test.ts (630). Station verification on rebased main: lint 0 / tsc 0 / arch:check 0 violations (362 files, 26 frozen modules) / unit 732/732 (+43) / architecture 402/402 (+13) / integration 763/763 (+17) CLEAN vs real embedded PostgreSQL (two earlier runs hit the pre-existing MKT-033 SIGTERM subprocess-exit race — 13/13 isolated ×4; flaked identically during MKT-040 verification on code that did not include these changes — environmental, documented). PR #27 squash-merged at 6856b25. |
| MKT-029 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-029, chat 14e654ec, GLM-5.3 agents tab, Full-Stack — the LAST of 40; delivered adf53aae (2 commits: work 577de31 + runbook) on base 5893799 (the 039 epoch); the worker pushed through its MinIO wall). The Agency Command Center (UI-001) as the MKT-030 precedent extended — /reporting module ADDITIVE EVOLUTION: public.ts + command-center-read-model + module + the ONE GET route /api/reporting/command-center/:agencyId. READ-ONLY BY CONSTRUCTION (single GET registration, zero mutating verbs in the route file, NO DTO at all → authority fields structurally unreachable). AGENCY-SCOPED SERVER-DERIVED (fresh membership resolution; uniform 404 foreign≡unknown≡malformed; suspended-membership 403 distinction; anonymous 401 fail-closed). The five authoritative surfaces: portfolio goals + per-client tallies, workflow state + instance recaps, evidence quality incl. low-grade counts, risks = declared constraints + failed/unresolved executions, pending approvals = running undecided experiment + blocked instance client-attributed. EVERY VIEW COUNT MATCHED AGAINST DIRECT SQL GROUND TRUTH; live-follow test (mutate through authorities → view follows, no staleness/caching); UI-AC-02 battery (verb 405s, authority-shaped body/query/header probes change nothing, durable rows BYTE-IDENTICAL after probes); member read posture (any ACTIVE member); boundary tests (route imports whitelisted — platform+authorize+application+/reporting public only; no projection state — NO migration, 032 stays reserved for MKT-019's ai_evaluations which took it; decision-room-boundary.test.ts evolution is a DISCLOSED additive widening — the /executions public contract joined the matrix-sanctioned import set, read-only verb scan extended to both reads). 12 files +3603/-36: src (reporting public+module+read-model+routes+wiring), 3 test suites (boundary + api 1179 + unit 727), runbook docs/implementation/MKT-029.md. Station rebase onto 65b564e (39/40): ZERO conflicts → b8ac0cd. SIX GATES on the rebased tree: lint 0 / tsc 0 / arch:check 0 violations / unit 742/742 (+10) / architecture 409/409 (+7) / integration 777/777 (+14) vs real embedded PostgreSQL — CLEAN FIRST RUN (MOS_TEST_MINIO override, disclosed: pinned 2025-09-07 MinIO release unobtainable post-reset — upstream 410 Gone — release-independent S3 protocol behavior verified via homebrew bottle 2025-10-15 binary with ELF PT_INTERP patch). PR #28 squash-merged at 9ab0168 — squash tree c426d86e BYTE-IDENTICAL to the verified b8ac0cd tree (confirmed post-merge on the fetched main). THE FINAL WORK ITEM. |

**🏁 v1.4 COMPLETE: ALL 40 OF 40 WORK ITEMS ACCEPTED/MERGED.** PRs #1–#28 squash-merged. Every delivery passed the six gates (lint / tsc / arch:check / unit / architecture / integration) plus independent Tech Lead diff review on a station clone before merge; the repository Git history is the sole architectural authority.

## v1.5 Phase — Operating Graph, Intelligence and App Ecosystem (MKT-041..MKT-052)

Architecture v1.5 FROZEN via PR #17 (squash-merged at b614485): Agency Operating Graph over canonical authorities, append-oriented Decision Ledger, derived Profit Intelligence, Client Operating Memory, AI Operator attention queue, sales-to-delivery continuity, and the App Ecosystem (immutable App Versions, workspace installs, trust levels UNVERIFIED/COMMUNITY_VERIFIED/MOS_CERTIFIED, upgrade/rollback semantics, bounded app-owned state). Deployment amended to validate required App/Extension capabilities. No core authority moves into Apps. Completion target: MKT-001..MKT-052 accepted/merged, with the final proof demonstrating Goal → Evidence → Hypothesis → Playbook → Deployment → Workflow → Task → Execution → Outcome → Decision → Learning → next action/deployment, including first-party and community-style App capability invocation, tenant isolation, versioned upgrade/rollback, real provider integration boundaries, profit/decision lineage, AI and human participation, and no alternate authorities.

| Work Item | State | Notes |
|---|---|---|
| MKT-041 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-041, chat a6864a90, GLM-5.3 agents tab, Full-Stack — v1.5 wave 1; the worker's platform turns died empty (capacity/limit pattern) after writing every file — its transcript ends at "Install dependencies with bun", sandbox reclaimed, NO completion report ever landed; the pushed branch was the delivery — repo-first rule). Agency Operating Graph (the DERIVED COORDINATION MODEL): 17 files +4910/−9 on EXACT base f2e6551, branch mkt/041-worker-delivery @ b02cfda. Migration 035_operating_graph.sql (operating_graph_nodes + operating_graph_edges ONLY): reference-only column inventory (canonical ids + kinds + scope chain — no authoritative shape), CHECK-fenced closed vocabularies (12 node kinds / 15 relations / the frozen five-value epistemic states unknown/observed/predicted/attributed/causal), scope-chain + cross-tenant endpoint fences (triggers), append-oriented versioned edges (per-client version uniqueness + single-current partial unique fence; the ONLY sanctioned UPDATE is the supersession transition; DELETE rejected — direct-SQL proven in tests), registry identity/scope immutability. src/modules/operating-graph: public.ts (frozen vocabularies; rebuildClientOperatingGraph module-level + getClientOperatingGraph + getAgencyOperatingGraph) + internal graph-projection (684-line pure derivation) + store (transactional convergence) + module (composes the 10 authority public contracts READ-ONLY via the disclosed frozen matrix line — every composed call is a read; writes ONLY the two derived tables). HTTP surface EXACTLY 2 GETs (agency portfolio rollup + client detail with version history); no mutating verb registered, no body/DTO/query surface — the rebuild is module-level, never a route. Spec registrations DISCLOSED minimal-additive: /operating-graph in §6 + matrix line + forbidden-direction bullet. Boundary test (436 lines): 2-GET proof, no-body proof, DML-against-only-own-tables proof, reference-only column inventory, CHECK-fence proof, uniform-404 tenant posture. Integration (1092 lines): one-rebuild relation set cross-checked against DIRECT SQL ground truth; second rebuild appends NOTHING (convergence); live-follow (new goal appends, REDEPLOY retracts the prior EXACT-VERSION pin — history stays addressable); vocabulary rejection at the DB; 401 anonymous / uniform 404 foreign / 405 mutating verbs; authoritative rows BYTE-IDENTICAL after every read. Station merge: main (MKT-042 decisions + MKT-047 apps) merged in — 7 shared-file conflicts resolved additively (spec §6 list with both additions; both matrix lines + forbidden bullets; application/routes/composition-root three-way coexistence; migration list 035<036<037; arch-check counts 28 spec-parsed / 29 enforced / 26 missing / 29 total; apps-boundary spec-count 27→28 with the operating-graph registration asserted). Station verification on the MERGED tree: lint 0 / tsc 0 / arch:check 0 violations (389 files, 29 enforced modules) / unit 794/794 (+7) / architecture 441/441 (+11) / integration 829/829 (+8) vs real embedded PostgreSQL — CLEAN FIRST RUN (MOS_TEST_MINIO override). PR #31 squash-merged at 8ecf3e3 from mkt/041-worker-delivery-rebased (tree equivalence verified: squash tree == station verification tree, byte-identical); the worker's original branch mkt/041-worker-delivery never rewritten. |
| MKT-042 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-042, chat 3eb11215, GLM-5.3 agents tab, Full-Stack — v1.5 wave 1; recovered once via Tech Lead refresh-icon + fresh-tab continuation after a dead turn mid-implementation). The append-oriented Decision Ledger (v1.5 change #2): 18 files +7498/−13 on EXACT base f2e6551. Migration 036_decisions.sql (decisions + decision_events ONLY): append-only triggers (event UPDATE/DELETE rejected, ledger DELETE rejected, proposal/audit rewrites rejected — direct-SQL proven in tests), CHECK-fenced frozen vocabulary (disposition enum with successor-shape + outcome-pairing constraints, execution XOR deployment reference, scope chains). src/modules/decisions: public.ts contract + store (§8 replay convergence: identical payload → 200 same rows, divergent → 409; disposition CAS exactly-one-winner) + module (write-time reference validation through /evidence /experiments /learnings /executions /deployments /clients /workspaces public contracts READ-ONLY via structural ports — fabricated/foreign refs rejected with zero rows). 6 routes POST/GET only; DTO rejects every proposer/provenance/lifecycle/outcome authority field; requireDecisionAccess fresh ownership chain + uniform 404 foreign≡unknown. Spec registrations DISCLOSED and verified minimal-additive (v1.4 promotion precedent — arch-check reads the module set from spec/architecture.md): /decisions in §6 module list + matrix dependency row; /policies reserved direction documented for MKT-045. Station verification: lint 0 / tsc 0 / arch:check 0 violations (374 files, 27 frozen modules) / unit 764/764 (+22) / architecture 420/420 (+11) / integration 803/803 (+26, real embedded PostgreSQL, MOS_TEST_MINIO override) — run 1 hit the documented MKT-033 SIGTERM environmental race (802/803), re-run clean 803/803. PR #29 squash-merged at 54a7af1. |
| MKT-047 | ACCEPTED/MERGED | Git-channel worker delivery (session mkt-047c, chat 5b0a57e7, GLM-5.3 agents tab, Full-Stack — v1.5 wave 1; THIRD attempt: first chat destroyed by platform before creation, second session's worker pod died mid-integration-testing with zero-length turns — voided; third landed and honored push-early: pushed at 42e0249 → 48ee185 → eca5c8f). The App registry authority for manifests: 16 files +5897/−6 on base f2e6551. src/modules/apps (public.ts contract + internal store/module) composes OVER /extensions via a READ-ONLY structural port (zero cross-module imports — the /deployments posture; arch-check enforces /apps with an EMPTY dependency allowance via the DISCLOSED checker.ts provision, removal path documented pending Architect ratification of the one-line spec §6 addition). Migration 037_apps.sql: apps + app_versions + app_dependencies — immutable published manifests (UPDATE/DELETE rejected by trigger — direct-SQL proven), REAL semver comparator (numeric ordering: 1.10.0 > 1.9.0), frozen certification enum born UNVERIFIED (developer self-certification 403 platform territory), namespace denylist (the 14 singular authorities), §21 material-key CHECKs, dependency validation triggers re-fencing the migration-028 extensions registry. 5 frozen routes (POST publish + GET reads; no update/delete surface); DTO guards reject authority-shaped keys 422 and certification-shaped keys 403; publisher identity server-derived. Station merge: main (MKT-042 decisions) merged in — 5 shared-file conflicts resolved additively (composition-root single modules-list line with both keys; migration list ordered 036<037; spec-count 27 and negative-fixture 25/28 test expectations updated for the 28-module enforced set). Station verification on the MERGED tree: lint 0 / tsc 0 / arch:check 0 violations (381 files, 28 enforced modules) / unit 787/787 / architecture 430/430 / integration 821/821 vs real embedded PostgreSQL — CLEAN FIRST RUN (MOS_TEST_MINIO override; the worker-disclosed 9 MinIO failures do not reproduce at station). PR #30 squash-merged at cc18bfb from mkt/047-worker-delivery-rebased (the worker's original branch never rewritten). |
| MKT-043 | ACCEPTED/MERGED | In-sandbox agent-lane worker delivery (Task ID 66-a, GLM-5.3 via Task-tool lane, Full-Stack — fourth attempt after three platform-incident losses on the chat channel; the chat.z.ai operator login was unavailable post sandbox-reset #4, so the resident Tech Lead dispatched through the in-sandbox agent lane with the identical dispatch contract, six-gate verification and diff review): branch mkt/043-worker-delivery (5dfa816 on base 4bbc542; intermediate 6212ddc from the interrupted first attempt recovered from origin per the push-early rule). Profit Intelligence as LIVE DERIVATION over canonical authorities — NO migration (the disclosed AC-4 choice; 038 reserved for the MKT-048 sibling). Exactly three GET surfaces (agency rollup / client / workspace slice), server-derived scope, uniform 404, zero mutation methods, zero SQL, frozen calculation vocabulary pi-calc-v1 + exported assumption set; every figure carries source refs + assumptions + version. Tech Lead station verification on the merged tree: lint 0, tsc 0, arch:check 0 violations (396 files), unit 816/816, architecture 454/454, integration 829 pass / 9 fail — all 9 the documented MinIO network-blocked environmental class (dl.min.io unreachable from this sandbox; .test-deps lost in reset #4), totals matching the worker's 838 with the known redis lock flake passing on the re-run; no-false-green honored. Diff review: additive-only registration (spec architecture.md §6 one line + dependency-matrix row + forbidden-direction bullet), 13 boundary architecture proofs, 22 unit + 9 integration module battery. PR #32 squash-merged at 990aa4b; squash tree 9414942b == station verification tree (byte-identical); worker branch never rewritten. |
| MKT-044 | ACCEPTED/MERGED | Replay-platform worker delivery (session mkt-044, GLM-5.3 agents tab, Full-Stack; dispatched into the freed slot after the 046 merge — base 3c0d759; delivery e64d046 + docs-only runbook 5254572 pushed during verification; PR #36 squash-merged at 29cae05). Client Operating Memory: governed client-context projection over canonical client/goal/playbook/deployment/evidence/experiment/outcome/decision/learning records — LIVE DERIVATION (no migration, the read-model precedent), frozen composition vocabulary cm-proj-v1 (governed selection rules disclosed, retrieval/index technology NON-authoritative per §6), exactly three GET surfaces (server-derived scope, uniform 404, fail-closed), zero SQL / zero mutation methods / no owned state. Tech Lead station verification: lint 0 / tsc 0 / arch:check 0 / unit 893/893 / architecture 502/502 / integration run CHUNKED (memory-safe via the immortal launcher after three silent OOM reaps of full-suite runners): chunk A 239/239 (includes the 9-test client-memory battery) + chunk B 634 pass + 9 MinIO environmental = 873 pass / 882 total. 13 boundary proofs; deletions all registration-count class. Tree equivalence byte-identical (3c9bb175). |
| MKT-045 | PENDING | AI Operator / Attention Queue. Deps: MKT-016/018/021 + MKT-041/042/043. |
| MKT-046 | ACCEPTED/MERGED | Replay-platform worker delivery (session mkt-046, chat 1c5fa1a1, GLM-5.3 agents tab, Full-Stack; two commits — delivery d4d025a + runbook b97ec83; base 98f20b3). Sales-to-Delivery Continuity: the §8 orchestrator carrying structured proposal scope/goals/outcomes/assumptions/economics into the Playbook/Deployment path THROUGH the existing creation commands (orchestrates, never duplicates; carried payload NEVER a request field — no manual re-entry). Migration 040_sales_continuity.sql: own tables ONLY (append-only carry ledger + event tail; identity-immutable / no-delete / forward-only completion ladder / cross-tenant reference fences). 5 GET + 2 POST surfaces; idempotent re-carry + version-bump identity. Tech Lead verification: six gates green on the branch (unit 832/832 / architecture 470/470 / integration 839 pass + 9 MinIO environmental); STATION MERGE with main (043+048+049) — 6 shared-file conflicts resolved additively (spec §6 four v1.5 registrations coexisting; arch-check 31 spec-parsed / 32 enforced; migrations 037<038<040; MKT-049 boundary count bumped 30→31 cross-delivery); gates re-run ALL GREEN on merged tree (unit 873/873 / architecture 489/489 / integration 864 pass + 9 MinIO). 16 boundary proofs; diff review clean (deletions all registration class). PR #35 squash-merged at 3f86766; tree equivalence verified byte-identical. |
| MKT-048 | ACCEPTED/MERGED | In-sandbox agent-lane worker delivery (Task ID 66-b, GLM-5.3 via Task-tool lane, Full-Stack — the worker session died before reporting but its pushed branch IS the delivery per the repo-first rule; driven to completion by a Tech Lead continuation resume). App Installation, Upgrade and Rollback: migration 038_app_installs.sql (own tables ONLY — app_installs ledger + app_install_events append-only tail; CHECK-fenced vocabularies; scope-chain + identity/grants + history-preserved triggers; append-only update/delete triggers; single sanctioned selection-supersession UPDATE, DELETE rejected). Six POST/GET surfaces, server-derived grants (DTO guard: scopes never caller-suppliable), exact-version historical identity + future-selection upgrade/rollback semantics, policy-gated. Tech Lead station verification: six gates green clean (lint 0 / tsc 0 / arch:check 0 violations 396 files / unit 804/804 / architecture 450/450 / integration 832 pass + 10 documented environmental fails — 9 MinIO + 1 SIGTERM race; app-installs battery 13/13). Post-043-merge station merge: main merged into the branch, 6 shared-file conflicts resolved additively (/app-installs + /profit-intelligence coexistence; arch-check counts 30 spec-parsed / 31 enforced), gates re-run ALL GREEN on merged tree (unit 826/826, architecture 463/463, integration 842 pass + 9 MinIO). PR #33 squash-merged at fd3fb71; squash tree 524070e == station merge tree 524070e (byte-identical); worker branch never rewritten beyond the disclosed station merge. |
| MKT-049 | ACCEPTED/MERGED | Replay-platform worker delivery (session mkt-049, chat 1b46bfaf, GLM-5.3 agents tab, Full-Stack; dispatched by the Tech Lead through dispatch_worker.py with prompt 100% insert-verified; two push-early snapshots 88bef1a + b0c8967). App SDK (tools/app-sdk): frozen-mirror types/vocabularies/semver/fingerprint, 1002-line offline validator (shares the registry validation source — drift test proves agreement), scaffolder, docs generator, typed client, CLI. Developer Portal: thin route family over the /apps authority (10 GET + 7 POST — delegated publishes; NEVER a second registry). OPTIONAL registry signing (hash attestation): server-computed canonical-manifest fingerprint equality, fail-closed 422 pre-write, backwards compatible (MKT-047 frozen schema untouched — disclosed in public.ts + runbook). NO-MIGRATION (SDK/portal are tooling + thin routes). Tech Lead station verification: lint 0 / tsc 0 / arch:check 0 violations / unit 857/857 / architecture 473/473 / integration 853 pass + 10 documented environmental fails (9 MinIO + 1 SIGTERM race). Diff review: PURE ADDITIVE (+6902 / −0, 22 files); SDK zero core-module imports (outbound artifact); portal imports platform + /apps publics only. PR #34 squash-merged at b01fef9; tree equivalence verified byte-identical; worker branch never rewritten. |
| MKT-050 | PENDING | App Marketplace, Trust and Certification. Deps: MKT-047/049. |
| MKT-051 | PENDING | Incumbent Capability App Program. Deps: MKT-047/048/049/050. |
| MKT-052 | PENDING | App Metering and Commercial Attribution. Deps: MKT-047/048/050. |
| MKT-040 amendment | PENDING | Deployment validation for required App/Extension capabilities, versions, permissions and compatibility before activation. Lands with the app-chain Work Item that needs it (MKT-047/048 epoch); Deployment still does not execute workflows or own app state. |

## Status meanings

- `PENDING`: dependencies not yet complete.
- `READY`: all dependencies accepted on current `main` and no known blocker.
- `IN_FLIGHT`: worker/PR actively implementing it.
- `REVIEW`: PR exists and awaits verification.
- `ACCEPTED`: objective acceptance evidence verified.
- `MERGED`: accepted changes are on `main`.
- `BLOCKED`: dependency, architecture or environment issue prevents safe execution.
- `RECONCILE`: repository evidence disagrees with this ledger; inspect before dispatch.

## Concurrency

Maximum active implementation workers: **3**.

Only dispatch concurrent Work Items when dependency satisfaction and expected file/authority surfaces demonstrate safe integration. Do not fill a worker slot merely to increase parallelism.

## Completion rule

Never mark a Work Item accepted because a worker reports success. Require objective evidence and inspect the repository itself.
