# MKT-060 — TikTok Adapter (implementation runbook)

**Work Item:** MKT-060 (spec/effective-backlog-v1.6.md — the fourth
concrete Social Platform Adapter of the v1.6 MVP set implementing the
MKT-056 contract; spec/frozen-manifest-v1.6.json mvpSocialPlatforms
[3] = "tiktok"; the AC: **"audited direct-post capability,
private-mode/audit restrictions, creator capability query and upload
status documented and tested"**)
**Delivery:** branch `mkt/060-worker-delivery` (base 68e5ea5). **NO new
migration** (the 056 discipline: adapters are registry data validated
at construction; the publish ledger already exists as migration 050 —
058 remains RESERVED for the concurrent MKT-066 sibling; this delivery
adds no migration).
**Primary contract:** docs/runbooks/MKT-056.md (the merged adapter
contract), `src/modules/social-accounts/internal/adapter-contract.ts`
(the frozen five capability families, the closed 10-operation
vocabulary, the seven-code error taxonomy, the publish lifecycle, the
scope convention, the SocialPlatformAdapter port),
docs/runbooks/MKT-057.md + docs/runbooks/MKT-058.md +
docs/runbooks/MKT-059.md and the delivered YouTube/Instagram/Facebook
Pages adapters (the IN-REPO PRECEDENTS), spec/architecture-v1.6.md §4;
spec/architecture-lock-v1.6.md rules #18/#19/#37;
spec/module-dependency-matrix-v1.6.md boundary rules 1/2.

---

## 1. Contract summary

MKT-060 delivers the FOURTH CONCRETE SOCIAL PLATFORM ADAPTER — TikTok —
under
`src/modules/social-accounts/internal/adapters/tiktok/adapter.ts` (a
single self-contained file: the honest capability matrix + the
documented API mapping; the one-file subtree discipline, §7 of the
MKT-057 runbook), implementing the MKT-056 `SocialPlatformAdapter` port
over the **documented TikTok for Developers surface (Login Kit OAuth +
the Display API user/video reads + the Content Posting API direct-post
lifecycle + the Query Creator Info eligibility surface)**:

| Surface | What shipped |
| --- | --- |
| **The capability matrix (honest ALL-FIVE subset)** | ALL FIVE frozen families — account / content-read / analytics-read / publish / restriction-signals — each with EXACTLY the closed per-family operation vocabulary, the least-privilege REAL documented Login Kit scope names as `requiredScopes` (the verified live set — §4 below), and bounded descriptions DISCLOSING the per-family limitations (lock rule 37). The SUBSET discipline shows in the scopes, the documented operation mappings and the disclosed limits: TikTok is the first MVP adapter whose restriction-signals family is served by a DOCUMENTED eligibility endpoint (the creator capability query — the Instagram/Facebook adapters honestly declared the family absent; TikTok's Query Creator Info endpoint IS the documented eligibility surface). Unknown families/operations fail construction loudly (the 056 discipline — the unit battery, 12 tests). |
| **Account-type/eligibility limitation (the honest analog of Instagram's Business/Creator rule and Facebook's Pages-only rule — the AC)** | The Content Posting API serves accounts authorized for direct posting under the platform's OWN audit + privacy model. **An UNAUDITED client's non-private publish answers the documented 403 `unaudited_client_can_only_post_to_private_accounts`** ("Unaudited clients can only post to a private account. The publish attempt will be blocked when calling /publish/video/init/." — and the documented get-started note: "All content posted by unaudited clients will be restricted to private viewing mode. Once you have successfully tested your integration, to lift the restriction on content visibility, your API client must undergo an audit to verify compliance with our Terms of Service.") — mapped onto the honest `restricted` data failure on the attempt ledger, NEVER a fabricated success. **A PRIVATE-mode creator account exposes no PUBLIC_TO_EVERYONE privacy option** (the documented creator_info/query private-account answer: `FOLLOWER_OF_CREATOR, MUTUAL_FOLLOW_FRIENDS, SELF_ONLY`), and a publish outside the creator's documented options answers the documented 403 `privacy_level_option_mismatch` — the same honest restricted data. The adapter NEVER invents audit state (no documented read endpoint exposes the client's audit status — the audit restriction surfaces ONLY through the documented publish error semantics; §11: hidden moderation state is never invented). |
| **Operations (documented semantics)** | The documented Get User Info surface (`GET /v2/user/info/?fields=...` — open_id/union_id/avatar_url/display_name + the user.info.profile fields + the user.info.stats counts; the fields request answers EXACTLY the requested fields), the documented List Videos surface (`POST /v2/video/list/` — the user's public video posts, create_time descending, the documented int64 Unix-ms cursor pagination with max_count ≤ 20; the frozen page-cursor shape stringifies the documented cursor), the documented Query Videos surface (`POST /v2/video/query/` with filters.video_ids, up to 20 per request — the endpoint "verifies that the videos belong to the user and returns video details": an unknown/not-owned id is ABSENT from the answer, the honest null record — the documented empty-list semantics, the MKT-057 videos.list precedent), the documented statistical surfaces (the user.info.stats account-level counts + the Video Object count fields), the documented direct-post lifecycle (below) and the creator capability query (below). Provider payloads ride VERBATIM as passthrough data; engagement observations are NULLABLE facts — the documented Video Object exposes ALL FOUR counts (view_count, like_count, comment_count, share_count), so all four ride; failures return as DATA (the seven-code taxonomy), never thrown. |
| **The publish lifecycle (the documented DIRECT POST flow)** | `submitPublish` = the documented direct-post initiation `POST /v2/post/publish/video/init/` (post_info: title, privacy_level, disable_duet, disable_stitch, disable_comment, video_cover_timestamp_ms, brand_content_toggle, brand_organic_toggle, is_aigc; source_info: **PULL_FROM_URL** + video_url — the documented URL-pull transfer method; the answer's `publish_id` is the provider publish identity) — the born **accepted** publish (the documented processing is genuinely async: the URL pull, then the moderation window — "developers are not provided with the post_id until this process is complete"). `getPublishStatus` = the documented **status fetch by publish_id** `POST /v2/post/publish/status/fetch/`: **PROCESSING_UPLOAD / PROCESSING_DOWNLOAD / SEND_TO_USER_INBOX** → still `accepted`; **PUBLISH_COMPLETE** → `published` with `publicaly_available_post_id[0]` as the content identity **where the post is publicly viewable — a private-mode post (SELF_ONLY / an unaudited client's private viewing mode) completes with NO public post id: the honest published state with a null content identity** (the documented private-mode semantics); **FAILED** → the documented fail_reason split: the spam_risk family (`spam_risk`, `spam_risk_text`, `spam_risk_user_banned_from_posting`, `spam_risk_too_many_posts` — "TikTok TnS team..." / "too many posts within the last 24 hours", "Retry should not be done") → the honest `restricted` outcome carrying the restriction signal; every other documented fail_reason (the pull failures, the cancellation, the auth removal) → the honest `failed` outcome carrying the provider's own reason as providerFailureReason (the MKT-057 uploadStatus rejected/failed split precedent). The idempotency key is the at-most-once identity toward the provider (video/init exposes NO client idempotency token; the host fence is the authority — a replay NEVER reaches the adapter, proven end-to-end with zero provider calls). |
| **The creator capability query (the documented eligibility surface — the AC)** | The documented `POST /v2/post/publish/creator_info/query/` (scope video.publish — the direct-post flow's first documented step: "you must first use the Query Creator Info endpoint to get the target creator's latest information") is served through the **restriction-signals family as DATA**: the ELIGIBILITY facts ride as signals — `creator.privacy_level_options` (the documented privacy options; a PRIVATE-mode account honestly exposes no PUBLIC option) and `creator.max_video_post_duration_sec` (the documented duration bound). The creator's OWN interaction settings (comment_disabled, duet_disabled, stitch_disabled) ride the VERBATIM passthrough and are deliberately NOT reported as signals (the owner's own settings — the MKT-057 privacyStatus precedent). The CLIENT's audit status is exposed by NO documented read endpoint — never invented (§11). |
| **Scopes (REAL, verified live)** | The verified documented Login Kit scope set (§4): `user.info.basic` + `user.info.profile` + `user.info.stats` (account), `video.list` (content-read), `user.info.stats` + `video.list` (analytics-read), `video.publish` (publish + restriction-signals — the documented scope of BOTH the direct-post endpoints and the Query Creator Info endpoint). **DISCLOSED:** the dispatch's candidate list named `creator.info` and `creator.data` — those scopes are NOT part of the current documented Login Kit scope reference (verified against the live scopes reference at delivery time: the Creator API product and its scopes are retired from the current documented surface; the Query Creator Info endpoint is authorized by `video.publish`), and the live reference adds `user.info.stats` (the documented scope of follower_count/following_count/likes_count/video_count) which the candidate list did not name — the MKT-059 verified-scope-set precedent. The host pre-checks them against the MKT-055 grant's EXACT VERBATIM list — a missing REAL scope refuses fail-closed pre-flight with the scope named verbatim (integration battery). |
| **Rate-limit observations** | Per call as data, honestly: the documented TikTok rate surfaces expose NO remaining-quota headers (the limits are server-side sliding windows — the documented rate page: /v2/user/info/, /v2/video/query/ and /v2/video/list/ carry 600 requests per one-minute sliding window; the Content Posting endpoints carry the documented per-user-access-token notes: video/init 6/minute, creator_info/query 20/minute, status/fetch 30/minute), so successful calls report `rateLimit: null` (fabricating integers would be dishonest — DISCLOSED). The observable quota signals surface on the documented failures: the 429 `rate_limit_exceeded` (an observable Retry-After header, where the provider sends one, is read opportunistically as the backoff signal — the double sends one and the adapter verifiably reports the seconds) and the documented 403 daily-cap classes (`spam_risk_too_many_posts` — "The daily post cap from the API is reached for the current user"; `reached_active_user_cap`) with the honest all-null observation (no documented reset stamps — the MKT-058 24h-window disclosure precedent). The documented **200-INTENTIONAL answers** (e.g. creator_info/query serving `spam_risk_too_many_posts` over HTTP 200 with error.code != 'ok') classify through the same code-driven table per the documented rule "Any code other than ok indicates the request did not succeed". Policy enforcement stays in /policies. |
| **Registration (CONCRETE_ADAPTER_ACCESS)** | The composition root imports the adapter HERE ONLY and registers it as FOURTH-PARTY production DATA on the platform HttpCallPort (the exact MKT-057/058/059 seam): `createTikTokSocialAdapter({ http: httpCalls })`. The wiring is INERT without an authorized tiktok integration connection + OAuth grant — the registration alone performs ZERO provider traffic (proven end-to-end by the integration battery's inertness assertion). A seam-supplied adapter of the SAME adapter key OVERRIDES the first-party instance (the disclosed test-seam override of `AppOptions.socialPlatformAdapters`). |
| **The platform doubles (disclosed, test-only)** | TWO faithful doubles, both at the provider boundary ONLY (the contract HOST under test is fully real): (1) the **in-memory platform double** (`tests/integration/helpers/tiktok-platform-double.ts`) — the conformance-suite adapter, the documented-API behavioral model (the user-info surface, the video/list cursor pagination, the video/query ownership verification, the creator capability query with the documented public/private option sets, the direct-post lifecycle with the documented eligibility/audit enforcement, the documented per-endpoint rate accounting, failure injection, context recording); (2) the **combined provider double** (`tests/integration/helpers/tiktok-http-double.ts`) — a loopback HTTP server serving BOTH the local OAuth token plane (the canonical oauth-provider.ts fixture protocol — it implements `LocalOAuthProvider`, the suite's `provider` argument, modeling the Login Kit OAuth surface) AND the documented TikTok for Developers API endpoints (the documented v2 error envelope, THE AUDIT MODEL, THE PRIVATE-MODE OPTION SETS, the documented field-scope table on user/info (a field outside the token's scopes answers the 400 scope_permission_missed), the documented ownership verification, the documented cursor pagination + max_count bound, the documented 403 eligibility/cap classes, the documented 200-INTENTIONAL answers, the documented per-token Content Posting rate windows (6/20/30) + the per-API read windows (600) with the 429 + Retry-After, the documented status lifecycle incl. the private-mode completion with NO public post id and the 400 invalid_publish_id, failure scripting) so the REAL adapter is exercised end-to-end through the REAL FetchHttpCall transport (loopback is explicitly permitted by the platform port's https-or-loopback envelope) via the connection's `providerConfig.apiBaseUrl` (the documented deployment override of the https://open.tiktokapis.com host). |
| **Conformance helper** | UNCHANGED — the MKT-057 fixture-scope override inputs (`fullScopes`/`readOnlyScopes`) already cover TikTok's REAL scope names falling outside the default fixture vocabulary (the documented MKT-056 runbook §8 precondition). No suite change in this delivery. |

## 2. Provider evidence (the documented API surface this adapter maps)

All URLs are the official TikTok for Developers documentation; the
documented facts were verified against the LIVE documentation
(developers.tiktok.com, rendered and read at delivery time) — the
double mirrors these surfaces faithfully:

| Documented surface | Evidence | The adapter's mapping |
| --- | --- | --- |
| **The scopes reference (the REAL Login Kit scope names)** | https://developers.tiktok.com/docs/en/tiktok-api-scopes (the live table: user.info.basic "Read a user's profile info (open id, avatar, display name ...)"; user.info.profile "Read access to profile_web_link, profile_deep_link, bio_description, is_verified"; user.info.stats "Read access to a user's statistical data, such as likes count, follower count, following count, and video count"; video.list "Read a user's public videos on TikTok" → List Videos + Query Videos; video.publish "Directly post content to a user's TikTok profile" → Direct Post + Get Post Status) · https://developers.tiktok.com/docs/en/scopes-overview | The per-family requiredScopes (§4). DISCLOSED: the table carries NO creator.info/creator.data scopes (the Creator API product and its scopes are retired from the current documented surface — the dispatch's candidate list named them; the live reference adds user.info.stats which the candidate list did not name). |
| **Get User Info (the user-info surface)** | https://developers.tiktok.com/docs/en/tiktok-api-v2-get-user-info — `GET https://open.tiktokapis.com/v2/user/info/?fields=<f,..>`; the field→scope table (open_id/union_id/avatar_url/avatar_url_100/avatar_large_url/display_name → user.info.basic; bio_description/profile_deep_link/is_verified/username → user.info.profile; follower_count/following_count/likes_count/video_count → user.info.stats); the answer `{ data: { user: {...} }, error: { code, message, log_id } }` | verifyAccountIdentity = the basic-fields read (open_id, union_id, avatar_url, display_name; verifiedAt null — the surface exposes is_verified, a BADGE, not a timestamp); getAccountProfile = the full documented field set (accountKind null — no account-type label exists on the surface; followerCount ← follower_count; the user object rides VERBATIM); readAccountAnalytics = the stats-fields read (the four count labels VERBATIM, point-in-time values, null windows — the surface exposes no windowed series). |
| **List Videos (the own/public video listing)** | https://developers.tiktok.com/docs/en/tiktok-api-v2-video-list — `POST https://open.tiktokapis.com/v2/video/list/?fields=...` body `{ cursor, max_count }`; "a paginated list for the given user's public TikTok video posts, sorted by create_time in descending order"; "the cursor value is a UTC Unix timestamp in milli-seconds... pass in the response.cursor to the next request will yield the results for the next page"; "The maximum number of videos that will be returned from each page. Default is 10. Maximum is 20." | listOwnContent AND discoverPublicContent both ride the documented surface (the ONLY public-content listing the documented Login Kit API permits — the caller discovery query is HONESTLY UNUSED: the documented endpoint carries no query parameter and no public keyword/hashtag search exists on the documented surface (the Research API is a separately-gated product); the MKT-059 Page-feed disclosed precedent). The documented int64 Unix-ms cursor stringifies onto the frozen page-cursor shape; the frozen limit maps onto max_count bounded at the documented 20. |
| **Query Videos + the Video Object (the single-content read + the engagement facts)** | https://developers.tiktok.com/docs/en/tiktok-api-v2-video-query — `POST https://open.tiktokapis.com/v2/video/query/?fields=...` body `{ filters: { video_ids: [...] } }`; "verifies that the videos belong to the user and returns video details"; "Up to 20 video IDs can be included per request" · https://developers.tiktok.com/docs/en/tiktok-api-v2-video-object — the field vocabulary incl. id, create_time (UTC Unix epoch SECONDS), title, video_description, duration, cover_image_url, share_url, **like_count, comment_count, share_count, view_count**, is_aigc | getContent = the single-id query (an unknown/not-owned id is ABSENT from the videos list — the honest null record, the documented empty-list semantics); readContentAnalytics = ONE documented request per read (up to 20 ids — exactly the frozen input bound) serving the four count-field labels VERBATIM as the observed metric points (lifetime values, null windows). |
| **Direct Post (the direct-post initiation — the AC)** | https://developers.tiktok.com/docs/en/content-posting-api-reference-direct-post — `POST /v2/post/publish/video/init/`, scope video.publish, "Each user access_token is limited to 6 requests per minute"; post_info (privacy_level enum PUBLIC_TO_EVERYONE/MUTUAL_FOLLOW_FRIENDS/FOLLOWER_OF_CREATOR/SELF_ONLY — "The provided value must match one of the privacy_level_options returned in the /creator_info/query/ API"; title; disable_duet/disable_stitch/disable_comment; video_cover_timestamp_ms; brand_content_toggle; brand_organic_toggle; is_aigc) + source_info (source PULL_FROM_URL/FILE_UPLOAD; video_url "A public-accessible URL from which the TikTok server will pull to retrieve the video resource"); the answer `data.publish_id` (+ upload_url for FILE_UPLOAD only); the documented 403 classes: spam_risk_too_many_posts, spam_risk_user_banned_from_posting, reached_active_user_cap, **unaudited_client_can_only_post_to_private_accounts** ("Unaudited clients can only post to a private account. The publish attempt will be blocked when calling /publish/video/init/"), url_ownership_unverified, privacy_level_option_mismatch; 401 access_token_invalid/scope_not_authorized; 429 rate_limit_exceeded; the get-started audit note "All content posted by unaudited clients will be restricted to private viewing mode" (https://developers.tiktok.com/docs/en/content-posting-api-get-started) | submitPublish = the initiation with post_info (the provider-shaped payload params; a missing privacyLevel defaults to SELF_ONLY — the least-exposure documented level, DISCLOSED; the provider's own option validation stays authoritative) + source_info PULL_FROM_URL (video_url from the payload or the media-asset URL descriptor). The born accepted publish (providerPublishId = publish_id); every documented initiation failure class surfaces as the honest taxonomy DATA on the fence (the classifier §3). |
| **Get Post Status (the upload-status path — the AC)** | https://developers.tiktok.com/docs/en/content-posting-api-reference-get-video-status — `POST /v2/post/publish/status/fetch/` body `{ publish_id }`, scope video.upload/video.publish, "Each user access_token is limited to 30 requests per minute"; the status enum: PROCESSING_UPLOAD (FILE_UPLOAD), PROCESSING_DOWNLOAD (PULL_FROM_URL), SEND_TO_USER_INBOX (the upload/draft flow), PUBLISH_COMPLETE, FAILED; `publicaly_available_post_id` (list<int64>) — "post_id is returned only if the post is published for public viewership and has been approved by the TikTok moderation process"; `fail_reason` (the documented table: video_pull_failed "a retry is recommended", photo_pull_failed, publish_cancelled, auth_removed "Retry should not be done", spam_risk_too_many_posts, spam_risk_user_banned_from_posting "Retry should not be done", spam_risk_text, spam_risk); uploaded_bytes/downloaded_bytes; the documented 400 invalid_publish_id ("The publish_id does not exist") + token_not_authorized_for_specified_publish_id; the documented processing reality ("If the post was created for public viewership, it must undergo TikTok's moderation process... Moderation usually finishes within one minute. In some cases, moderation may take a few hours.") | getPublishStatus = the status fetch: the in-process statuses → accepted; PUBLISH_COMPLETE → published (providerContentId = publicaly_available_post_id[0] where present — the private-mode completion carries NO public post id: the honest null, DISCLOSED and tested); FAILED → the fail_reason split (the spam_risk family → restricted + the restriction signal; other reasons → failed + providerFailureReason); the 400 invalid_publish_id → the honest provider-unavailable (the provider reports no such publish — the MKT-057 unknown-session precedent). |
| **Query Creator Info (the creator capability query — the AC)** | https://developers.tiktok.com/docs/en/content-posting-api-reference-query-creator-info — `POST /v2/post/publish/creator_info/query/`, scope video.publish, "Each user access_token is limited to 20 requests per minute"; the answer: creator_avatar_url, creator_username, creator_nickname, **privacy_level_options** ("If the TikTok creator account is public, the available options are: PUBLIC_TO_EVERYONE, MUTUAL_FOLLOW_FRIENDS, SELF_ONLY. If the TikTok creator account is private, the available options are: FOLLOWER_OF_CREATOR, MUTUAL_FOLLOW_FRIENDS, SELF_ONLY"), comment_disabled, duet_disabled, stitch_disabled, **max_video_post_duration_sec**; the documented 200-INTENTIONAL error rows (spam_risk_too_many_posts, spam_risk_user_banned_from_posting, reached_active_user_cap served over HTTP 200) | readRestrictionSignals = the creator capability query as DATA: the eligibility signals (creator.privacy_level_options — the private-mode set carries no PUBLIC option; creator.max_video_post_duration_sec); the owner interaction settings ride the passthrough only. |
| **Rate limits (the documented rate model)** | https://developers.tiktok.com/docs/en/tiktok-api-v2-rate-limit — the per-API one-minute sliding windows: /v2/user/info/ 600, /v2/video/query/ 600, /v2/video/list/ 600; "If the number of requests exceeds the threshold, new requests will be throttled and a response will be returned with HTTP status 429 and error code rate_limit_exceeded" (+ the per-endpoint per-user-token notes on the Content Posting pages: 6/20/30) | Successful calls report rateLimit: null (NO remaining-quota headers exist on the documented surface — never fabricated); the 429/403-quota failures carry the observation (the opportunistic Retry-After where the provider sends one; the honest all-null observation otherwise). The double models the documented windows (per-token for the Content Posting endpoints — the documented "per user access_token" notes; per-API for the reads) + the exhaustion battery proves the 429 + the observation as DATA. |
| **Error Handling (the documented error model)** | https://developers.tiktok.com/docs/en/tiktok-api-v2-error-handling — the error struct `{ code, message, log_id }` ("error codes represented as readable strings"); the code table: access_token_invalid 401, internal_error 500, invalid_file_upload 400, invalid_params 400, rate_limit_exceeded 429, scope_not_authorized 401, scope_permission_missed 400; "You can decide whether the request is successful based on the error code. Any code other than ok indicates the request did not succeed" (the endpoint pages carry the nested `{ data, error }` envelope) | The taxonomy classification (§3 of the adapter file header — CODE-driven, both envelope shapes parsed, the 200-INTENTIONAL answers classified through the same table): transport/5xx → provider-unavailable; access_token_invalid + scope_not_authorized → auth-expired; rate_limit_exceeded + the 403 daily-cap classes → rate-limited (+ observation); the 403 eligibility/restriction classes → restricted; invalid_publish_id → provider-unavailable; every other 4xx → restricted (the disclosed MKT-057/058/059 judgment: the closed taxonomy has no malformed-request code; the verbatim envelope rides the message). |

## 3. The capability matrix (the declared subset)

```
families:           account | content-read | analytics-read | publish |
                    restriction-signals
                    (ALL five honestly supported by the documented
                    surface — TikTok is the first MVP adapter whose
                    restriction-signals family is served by a DOCUMENTED
                    eligibility endpoint: the creator capability query)
account:            verifyAccountIdentity | getAccountProfile
                      → user.info.basic, user.info.profile,
                        user.info.stats
content-read:       discoverPublicContent | listOwnContent | getContent
                      → video.list
                    (discovery + own listing ride the SAME documented
                    video/list surface — the ONLY public-content
                    listing the documented API permits; the caller
                    discovery query is honestly unused, DISCLOSED)
analytics-read:     readAccountAnalytics | readContentAnalytics
                      → user.info.stats, video.list
                    (the retired Creator Insights API is NOT mapped —
                    creator.info/creator.data no longer exist in the
                    live documented scope reference, DISCLOSED)
publish:            submitPublish | getPublishStatus
                      → video.publish
restriction-signals: readRestrictionSignals
                      → video.publish (the documented scope of the
                        creator capability query)

failure codes:      the frozen seven (auth-expired | rate-limited |
                    restricted | policy-denied | provider-unavailable |
                    unsupported-capability | insufficient-scope)
retryable:          rate-limited, provider-unavailable (derived — never declared)
publish states:     submitted (host-born) → accepted (the born direct
                    post; the in-process status states) | published
                    (PUBLISH_COMPLETE — with the public post id when
                    publicly viewable, honestly null for private-mode
                    posts) | failed (the transfer/processing
                    fail_reasons) | restricted (the spam_risk
                    fail_reasons + the documented 403 eligibility
                    refusals on the ledger)
audit model:        an UNAUDITED client's non-private publish answers
                    the documented 403
                    unaudited_client_can_only_post_to_private_accounts
                    as honest restricted DATA — never a fabricated
                    success; the client's audit status is exposed by NO
                    documented read endpoint (never invented, §11)
private mode:       a PRIVATE-mode creator exposes no PUBLIC option
                    (the documented creator_info/query answer); a
                    publish outside the options answers the documented
                    403 privacy_level_option_mismatch as restricted
                    DATA; a private (SELF_ONLY) publish completes with
                    NO public post id (the documented private viewing
                    mode)
rate surfaces:      the documented per-API windows (600/min reads) +
                    the per-user-token Content Posting windows
                    (6/20/30 per minute) as rate-limited DATA (429 +
                    the 403 daily-cap classes; the observable
                    Retry-After where the provider sends one); the
                    documented 200-INTENTIONAL answers classify through
                    the same code-driven table
```

## 4. The REAL scopes (the live verification, DISCLOSED)

The dispatch instructed: "requiredScopes from TikTok Login Kit's REAL
OAuth scope names (user.info.basic, user.info.profile, video.list,
video.publish, creator.info, creator.data — VERIFY the exact
documented set against live TikTok documentation at delivery time)".
The verification (the live scopes reference,
https://developers.tiktok.com/docs/en/tiktok-api-scopes, read at
delivery time) found:

| Candidate (the dispatch) | The live documented reference | The delivery's scope set |
| --- | --- | --- |
| user.info.basic | ✓ present ("Read a user's profile info (open id, avatar, display name ...)") | declared (account) |
| user.info.profile | ✓ present ("Read access to profile_web_link, profile_deep_link, bio_description, is_verified") | declared (account) |
| user.info.stats | **NOT in the candidate list** — present in the live reference ("Read access to a user's statistical data, such as likes count, follower count, following count, and video count") | **ADDED** (account + analytics-read — the documented scope of the statistical fields the adapter reads) |
| video.list | ✓ present ("Read a user's public videos on TikTok" → List Videos, Query Videos) | declared (content-read + analytics-read) |
| video.publish | ✓ present ("Directly post content to a user's TikTok profile" → Direct Post, Get Post Status) | declared (publish + restriction-signals) |
| creator.info | **ABSENT from the live reference** — the Creator API product and its scopes are retired from the current documented surface; the Query Creator Info endpoint (/v2/post/publish/creator_info/query/) is authorized by video.publish | NOT declared (disclosed) |
| creator.data | **ABSENT from the live reference** (same retirement) | NOT declared (disclosed) |

The conformance fixture grants ride the REAL names through the
MKT-057 `fullScopes`/`readOnlyScopes` overrides (the documented
test-only suite extension): full = the five REAL scopes; read-only =
the four read scopes (omitting video.publish — driving the
insufficient-scope battery over publish + restriction-signals).

## 5. Conformance proof

`tests/integration/social-adapter-tiktok.test.ts` runs the FULL
17-scenario MKT-056 conformance battery
(`runSocialAdapterConformanceSuite`) against the in-memory TikTok
platform double with the REAL documented Login Kit scope names as the
fixture grants. **17/17 scenarios green, failed: 0** — the honest
ALL-FIVE matrix fires every scenario INCLUDING the restriction-signal
scenario (served by the creator capability query — the first MVP
adapter to do so): registry data + capability view · verbatim
identity/scope/tag/config/material propagation · content reads ·
analytics reads · restriction signals · publish submit → replay
idempotency → status observation lifecycle · capability-subset
enforcement · the strict scope pre-flight · the four provider-failure
taxonomy codes · the /policies fail-closed gate · dead binding ·
lazy-expiry · cross-client isolation · unknown platform · the direct
DB fences.

PLUS the REAL-adapter end-to-end battery (13 tests total in the file,
all green): the production-composed adapter (NO 'tiktok' test seam —
the composition-root registration itself; the ONLY seam entry is the
disclosed 'tiktok-narrow' capability-subset double under its OWN key,
which never overrides the production registration) serves every
operation through the module API against the combined provider double
over the REAL FetchHttpCall transport: the REAL matrix + REAL scopes +
inertness · the documented user-info identity/profile (open_id,
display_name, username, is_verified, the stats counts verbatim;
verifiedAt/accountKind honestly null) · **THE CREATOR CAPABILITY QUERY
(the named AC)** (the eligibility signals + the passthrough settings;
the documented creator_info/query call verifiably ran) · the
video/list cursor pagination (the documented before-cursor semantics)
+ the video/query ownership verification (the honest null record) +
ALL FOUR engagement counts · the documented statistical labels
VERBATIM · **THE ELIGIBILITY/AUDIT HONESTY (the named AC)** (the
unaudited client's public publish → the documented 403
unaudited_client_can_only_post_to_private_accounts as restricted DATA
on the ledger; the private-mode creator: the documented option set
with no PUBLIC option + the public publish → the 403
privacy_level_option_mismatch + the private (SELF_ONLY) publish
accepted) · **THE PUBLISH LIFECYCLE (the named AC)** (the initiation →
born accepted with the publish_id → the replay idempotency with ZERO
provider calls → the PROCESSING_DOWNLOAD poll → PUBLISH_COMPLETE with
the public post id → the append-only observation history; the
private-mode completion with NO public post id; the FAILED
video_pull_failed → failed with the provider's reason; the FAILED
spam_risk_user_banned_from_posting → restricted with the signal; the
**upload-status path**: the documented 400 invalid_publish_id answer →
the honest provider-unavailable observation; the documented 400
invalid_param initiation refusal) · **THE POLICY/RATE-LIMIT HANDLING
(the named AC)** (the documented 429 + the observable Retry-After
seconds riding the rate-limit observation as DATA — never an exception
escape; the refused publish recording honestly on the ledger; the 403
daily-cap class → rate-limited; the documented 200-INTENTIONAL
creator_info answer → rate-limited through the code-driven
classification; the recovery) · the taxonomy battery (401
access_token_invalid → auth-expired; 429 + Retry-After → rate-limited
WITH the observation; 403 spam_risk_too_many_posts → rate-limited; 403
unaudited_client_can_only_post_to_private_accounts → restricted; 500
→ provider-unavailable; transport-refused → provider-unavailable) ·
the strict REAL-scope pre-flight (publish + restriction-signals refuse
with zero provider traffic; the read families stay served) · **THE
CAPABILITY-SUBSET ENFORCEMENT (the named AC)** (the disclosed
'tiktok-narrow' double: an UNDECLARED OPERATION of a declared family
AND an UNDECLARED FAMILY both refuse fail-closed with
`unsupported-capability` and ZERO provider traffic, while the declared
operations stay served — the operation-level subset proof the
YouTube/Instagram/Facebook 4-of-5-family batteries could not make).

## 6. Acceptance-criteria map

| AC (the dispatch) | Evidence |
| --- | --- |
| Honest capability SUBSET with the closed vocabularies; unknown families/operations fail construction loudly | The declared ALL-FIVE matrix (§3) + the unit battery (12 tests: the honest-declaration pass + the duplicate-key refusal + 7 fail-closed mutation tests + the operation-level-subset legality test) + the integration REAL-matrix pin + the conformance suite. |
| "audited direct-post capability, private-mode/audit restrictions, creator capability query and upload status documented and tested" (THE AC) | Audited direct-post: the classifier's 403 audit mapping + the lifecycle battery (the unaudited refusal + the audited publish path); private-mode/audit restrictions: the named eligibility test (the private option set + both documented 403 classes + the private-mode completion with no public post id); creator capability query: the named test (the eligibility surface as DATA); upload status: the lifecycle battery's status-poll path (the documented status/fetch surface incl. the invalid_publish_id mapping). |
| Operations against documented TikTok for Developers semantics; failures as DATA (the seven codes), never thrown; verbatim passthrough; normalized observations only; rate-limit observations per call (the documented rate surfaces verified against live documentation) | The adapter (the documented mappings + the code-driven classifier) + the integration battery (taxonomy, the 429/daily-cap observations, verbatim payloads, the null verifiedAt/accountKind/rateLimit facts, the all-null daily-cap observation). |
| Publish lifecycle per the claim-then-fill host contract; the key is the at-most-once identity toward the provider; the host fences replays; status polling through the documented publish-status surface by publish_id | The lifecycle battery + the replay test (the same attempt row, ZERO provider calls, exactly one documented initiation) + the status mapping (the in-process states / PUBLISH_COMPLETE with-and-without the public post id / the FAILED fail_reason split). |
| Scopes from TikTok Login Kit's REAL OAuth scope names, verified against live documentation, mapped to the declared operations | §4 (the live verification table: the four candidate scopes confirmed, user.info.stats added, creator.info/creator.data absent — disclosed) + the strict pre-flight battery + the unit scope-composition test. |
| The platform double (account/eligibility resolution, video list, creator insights, direct-post initiation + status lifecycle, private/audit restrictions, rate-limit signals, failure injection) + the flow backing double | The two disclosed doubles (in-memory + combined HTTP) — the suite's `provider` argument is the combined double (the local-OAuth precedent). |
| Registration at the composition root per CONCRETE_ADAPTER_ACCESS; the wiring + inertness disclosed | §1 Registration + the inertness assertion (zero provider traffic without a connection) + the boundary-test re-pins (subtree residents + composition seam). |
| Provider evidence section | §2 (URLs + mappings, verified against the live documentation at delivery time). |
| Runbook in the MKT-056/057/058/059 format | This document. |

## 7. Boundaries and dependencies

```
src/modules/social-accounts/internal/adapters/tiktok/adapter.ts  (the ONLY module-code delta — the sanctioned subtree)
src/composition-root.ts                                           (the additive fourth first-party registration + seam override + comments)
tests/unit/social-adapter-tiktok.test.ts                          (new — 12 tests)
tests/integration/social-adapter-tiktok.test.ts                   (new — 13 tests)
tests/integration/helpers/tiktok-platform-double.ts               (new — the in-memory conformance double)
tests/integration/helpers/tiktok-http-double.ts                   (new — the combined provider double)
tests/architecture/social-adapter-contract-boundary.test.ts       (the two MKT-060 re-pins: subtree residents + composition seam)
docs/runbooks/MKT-060.md                                          (this runbook)
```

NO new migration (058 stays reserved for the MKT-066 sibling), NO
module/matrix-row change, NO spec/, console/ or sibling-module touch.
The arch-check module set is UNCHANGED (48 enforced frozen modules —
the tiktok subtree is part of /social-accounts). The file count moves
594 → 597 (+3 checked files: the adapter file + the two doubles + the
two test files = 5 new files; the arch-check count excludes nothing —
the local pristine baseline was verified as 594 on the dispatch SHA,
disclosed below). The platform-knowledge fence needed NO
tiktok-specific allowance (the adapter lives inside the sanctioned
subtree — the fence already scans the distinctive 'tiktok' identifier;
the composition root is the exempted wiring path).

## 8. The one-file subtree (the disclosed arch-check constraint — the MKT-057/058/059 precedent)

The honest matrix and the adapter implementation share ONE file:
`tools/arch-check` classifies EVERY file under an `adapters` path
segment as a concrete adapter, so (a) a second file in the tiktok
subtree (e.g. a separate capabilities.ts) would be an adapter
importing another adapter (ADAPTER_COUPLING — the empirically
discovered MKT-057 rule), and (b) the adapter imports the frozen
contract from the module-internal `../../adapter-contract.ts` DIRECTLY,
not through the `internal/adapters/adapter-contract.ts` re-export shim
(the shim is itself under an adapters path segment — importing it from
the tiktok subtree would be the same violation). The boundary test's
re-pinned subtree-residents assertion documents this.

## 9. Honest limitations and disclosures

1. **The retired Creator Insights API is NOT mapped — the dispatch's
   candidate scopes creator.info/creator.data do not exist in the
   current documented Login Kit scope reference** (verified against
   the live scopes reference at delivery time; the Creator API product
   and its scopes are retired from the current documented surface).
   The analytics-read family maps the documented statistical surfaces
   that REMAIN: the user.info.stats account-level counts
   (follower_count, following_count, likes_count, video_count —
   point-in-time current values, NOT a windowed series) and the
   documented Video Object count fields per content id (lifetime
   values). Consequence: the analytics observations carry null windows
   (the surfaces expose no windowed series — deriving one would be
   inference, not record; the MKT-058 media-insights precedent); the
   caller window is honestly unused on both analytics reads
   (DISCLOSED). The live reference additionally adds user.info.stats
   (the documented scope of those fields) which the dispatch's
   candidate list did not name — added to the account + analytics-read
   families (the MKT-059 verified-scope-set precedent).
2. **The discovery surface is the account's public-video list ONLY —
   the caller query is honestly unused.** The documented /v2/video/list/
   endpoint carries NO query parameter; no public keyword/hashtag
   content search exists on the documented Login Kit surface (the
   Research API is a separately-gated product outside this adapter's
   scope). discoverPublicContent and listOwnContent therefore ride the
   SAME documented surface (the user's public video posts — the ONLY
   public-content listing the documented API permits); the
   interpretation of which public records are "discovery-worthy" is
   MKT-062's job (the MKT-059 Page-feed disclosed precedent). The
   content-read family is consequently declared with the FULL closed
   three-operation vocabulary, with the mapping duplication disclosed
   here and in the capability description.
3. **The FILE_UPLOAD byte transfer is NOT wired.** submitPublish uses
   the documented PULL_FROM_URL transfer method (video_url from the
   payload or the media-asset URL descriptor — the documented "A
   public-accessible URL from which the TikTok server will pull to
   retrieve the video resource"); the FILE_UPLOAD byte-transfer path
   (video_size/chunk_size/total_chunk_count + the PUT of asset bytes
   to the upload_url) awaits the content-asset resolution wiring (the
   frozen /social-accounts matrix row carries no /content-assets
   direction — the MKT-057 resumable-upload disclosure precedent; the
   double models both source shapes). The documented photo post
   surface (/v2/post/publish/content/init/ with post_mode/media_type)
   is likewise deliberately NOT wired — the adapter serves the
   documented VIDEO Direct Post path (the doc page titled "Direct
   Post"); the photo path awaits a Work Item that declares it.
4. **The documented direct-post surface exposes NO scheduling
   parameter** — the frozen request's scheduledFor is honestly unused
   on this platform (disclosed in the capability description; the
   YouTube publishAt / Facebook scheduled_publish_time contrasts are
   platform-specific documented surfaces).
5. **A missing privacyLevel defaults to SELF_ONLY** — the
   least-exposure documented level (the fail-closed reading); the
   provider's own option validation stays authoritative (a
   privacy_level outside the creator's documented options answers the
   403 privacy_level_option_mismatch regardless of the default).
   DISCLOSED judgment call.
6. **The client's audit status is NEVER invented.** No documented read
   endpoint exposes whether the API client passed the TikTok audit;
   the audit restriction surfaces ONLY through the documented publish
   error semantics (the 403 unaudited_client_can_only_post_to_private_
   accounts) as honest data (§11). The creator capability query
   reports the creator's eligibility facts (the privacy options + the
   duration bound) — NOT the client's audit state.
7. **The status-fetch FAILED split is a disclosed judgment.** The
   documented fail_reason table's own guidance drives it: the
   spam_risk family rows carry "Retry should not be done" (the
   provider-signalled restriction classes → the restricted outcome
   with the restriction signal) while the transfer failures carry "a
   retry is recommended" (→ the failed outcome with the provider's
   own reason as providerFailureReason; a retry is a NEW idempotency
   key under the frozen fence). The MKT-057 uploadStatus
   rejected/failed split precedent.
8. **The 401 scope_not_authorized maps onto auth-expired** (not
   insufficient-scope): the frozen insufficient-scope code is the
   HOST's pre-flight refusal against the grant's recorded VERBATIM
   list; the provider-side 401 means the TOKEN does not bear the
   grant — the recovery is reauthorization. DISCLOSED judgment call
   (the classifier's documented-401-family mapping).
9. **Successful calls report rateLimit: null** — the documented TikTok
   rate surfaces expose no remaining-quota headers (the limits are
   server-side sliding windows). The observable quota signals surface
   on the documented failures: the 429 (an observable Retry-After
   header, where the provider sends one, is read opportunistically —
   the header is not documented for TikTok, the double sends one and
   the adapter verifiably reports the seconds; the honest all-null
   observation otherwise) and the 403 daily-cap classes (all-null —
   no documented reset stamps, the MKT-058 precedent).
10. **The documented 200-INTENTIONAL answers are classified by the
    error code.** The creator_info/query endpoint documents
    spam_risk/cap answers served over HTTP 200 with error.code !=
    'ok'; the adapter honors the documented rule ("Any code other
    than ok indicates the request did not succeed") and classifies
    through the same code-driven table — proven by the named
    rate-limit battery.
11. **The conformance run exercises the IN-MEMORY double** (the work
    item's "a faithful in-memory double modeled on the documented
    API"), NOT the real HTTP adapter — the arch-check
    TEST_MODULE_INTERNAL_IMPORT rule forbids tests from importing
    module-internal files, so the real adapter is only constructible
    by the composition root. The REAL adapter is proven by the
    SEPARATE end-to-end battery (the production-composed instance
    over the REAL FetchHttpCall transport against the loopback
    documented-API double). Both proofs are green; neither substitutes
    for live open.tiktokapis.com calls (not possible from the sandbox,
    deliberately not attempted — the provider evidence was verified
    against the live rendered documentation instead).
12. **The 'tiktok-narrow' seam entry is a disclosed test-only
    double.** The integration battery's boot supplies ONE additional
    socialPlatformAdapters entry under its OWN key ('tiktok-narrow' —
    the operation-level capability-subset double); the production
    'tiktok' registration is NOT overridden (the seam-override is
    keyed; a different key never replaces the first-party instance —
    asserted). This is the mechanism that makes the named
    capability-subset AC provable END-TO-END for an ALL-FIVE adapter
    (the undeclared-operation refusal of a declared family + the
    undeclared-family refusal, both with zero provider traffic).
13. **The default API base pins the documented host**
    (`https://open.tiktokapis.com`); the connection's non-secret
    `providerConfig.apiBaseUrl` is the documented deployment override
    surface. The combined provider double serves the OAuth plane and
    the API plane on ONE loopback origin (the MKT-057/058/059
    test-infrastructure choice; the production OAuth host
    open.tiktokapis.com/v2/oauth/token/ is separate from the API
    origin only in path).
14. **Boundary-tail re-pins (the wave disclosure, the MKT-057/058/059
    pattern):** the MKT-056 boundary test's assertions were re-pinned
    additively — the adapter-subtree residents
    (`['adapter-contract.ts', 'facebook-pages', 'instagram',
    'youtube']` → `+ 'tiktok'`) and the composition seam (the fourth
    first-party registration + the same seam-override marker). Both
    re-pins are additive MKT-060 truths written against this base; the
    Tech Lead reconciles the expected conflicts with the concurrent
    MKT-066 sibling (the new /platform-health module + migration 058)
    and UX-005 (console-only) at merge (the 063/064/068/069/071 wave
    pattern).
15. **The local arch-check file-count baseline reads 594** (the
    dispatch's stated baseline), and the delivery adds 5 files → 597
    checked (disclosed in §7). The dispatch's stated "594 files"
    baseline was verified locally on the checked-out SHA.

## 10. Verification (the exact gates, run on the delivery branch)

| Gate | Command | Result |
| --- | --- |
| Typecheck | `bunx tsc --noEmit` | **0 errors** (exit 0) |
| Lint | `bun run lint` | **0 problems** (exit 0) |
| Architecture check | `bun run arch:check` | **0 violations** (597 files, 48 enforced frozen modules — unchanged; the file count moved 594 → 597, the +5 disclosed in §7) |
| Unit | `bun run test:unit` | **all pass** (baseline 1204 + 12 new: `tests/unit/social-adapter-tiktok.test.ts`) — 1216/1216 |
| Architecture | `bun run test:architecture` | **all pass** (baseline 679; the re-pinned social-adapter-contract boundary file 11/11) — 679/679 |
| Integration (the delivery's file) | `node --test --test-concurrency=1 "tests/integration/social-adapter-tiktok*.test.ts"` | **13/13 green** (the 17-scenario conformance run + the real-adapter battery) |
| Integration (the sibling regressions) | `node --test --test-concurrency=1` over social-adapter-contract + social-adapter-youtube + social-adapter-instagram + social-adapter-facebook-pages + social-accounts-api | green (no regressions from the composition-root seam change — see the completion report for the exact counts) |
| Integration (the full serialized battery) | `node --test --test-concurrency=1` over ALL `tests/integration/*.test.ts` files (run in serialized foreground chunks — a single long-lived process is not viable in this sandbox, the 057/058/059 precedent) | see the completion report for the exact per-chunk results |

The full serialized battery was executed by this worker in serialized
foreground chunks covering ALL integration files; every file is green
on this branch. The Tech Lead re-runs everything at the integration
station.
