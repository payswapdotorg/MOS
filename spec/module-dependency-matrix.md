# MarketingOS Module Dependency Matrix

**Architecture Version:** 1.1
**Status:** FROZEN

The arrows below are allowed dependency directions. A module may depend on a declared public contract of a downstream module only where listed. Direct imports of another module's `internal/` implementation are forbidden.

```text
/auth ──→ /users
/agencies ──→ /users, /auth
/clients ──→ /agencies, /auth
/workspaces ──→ /clients
/goals ──→ /clients, /workspaces
/playbooks ──→ /agencies, /clients, /goals
/workflows ──→ /workspaces, /goals, /playbooks, /executions, /policies, /audit
/executions ──→ /workspaces, /policies, /credentials, /audit
/agents ──→ /executions, /ai-runtime, /policies
/field-agents ──→ /users, /clients, /policies
/jobs ──→ /workflows, /executions, /field-agents, /clients, /evidence, /policies
/evidence ──→ /clients, /workspaces, /executions
/metrics ──→ /evidence, /integrations
/experiments ──→ /evidence, /metrics, /goals
/learnings ──→ /evidence, /experiments, /goals
/integrations ──→ /credentials, /policies
/extensions ──→ /executions, /policies, /credentials, /audit
/ai-runtime ──→ /executions, /policies, /credentials, /evidence
/policies ──→ /clients, /agencies
/credentials ──→ /auth, /policies
/audit ──→ /auth
/notifications ──→ /auth, /audit
/reporting ──→ /goals, /workflows, /executions, /evidence, /experiments, /metrics, /learnings
/operating-graph ──→ /clients, /workspaces, /goals, /playbooks, /workflows, /executions, /deployments, /evidence, /experiments, /learnings
/decisions ──→ /evidence, /experiments, /learnings, /executions, /deployments, /policies, /clients, /workspaces
/app-installs ──→ /apps, /policies, /workspaces, /extensions
/profit-intelligence ──→ /clients, /workspaces, /goals, /playbooks, /workflows, /executions, /deployments, /evidence, /metrics, /jobs, /field-agents, /ai-runtime, /integrations
/ai-operator ──→ /clients, /workspaces, /workflows, /executions, /deployments, /jobs, /policies, /evidence, /experiments, /learnings, /field-agents, /profit-intelligence
/sales-continuity ──→ /decisions, /playbooks, /deployments, /clients, /workspaces, /evidence
/client-memory ──→ /clients, /workspaces, /goals, /playbooks, /deployments, /evidence, /experiments, /decisions, /learnings
/app-marketplace ──→ /apps
/app-metering ──→ /apps, /app-installs, /extensions, /workspaces
/first-party-apps ──→ /apps, /app-installs, /reporting, /profit-intelligence, /clients, /workspaces, /decisions, /evidence, /metrics, /integrations
/growth-missions ──→ /agencies, /goals
/social-accounts ──→ /integrations, /credentials, /policies, /workspaces
/product-intelligence ──→ /research, /evidence, /integrations, /ai-runtime
/notification-delivery ──→ /notifications, /policies, /credentials
/growth-operator ──→ /growth-missions, /goals, /playbooks, /deployments, /workflows, /executions, /evidence, /experiments, /learnings, /decisions, /policies
/content-rights ──→ /evidence, /policies, /content-assets
/content-assets ──→ /executions, /content-rights
/experiment-analysis ──→ /experiments, /metrics, /evidence, /learnings
/cross-platform-distribution ──→ /growth-missions, /social-accounts, /content-assets, /content-rights, /integrations, /policies
/research ──→ /integrations, /evidence, /ai-runtime
/content-intelligence ──→ /evidence, /metrics, /experiments, /integrations, /research
```

## Forbidden dependency directions

- `/ai-runtime` must not import a concrete provider SDK outside its adapter implementation.
- `/workflows` must not depend on a model/provider implementation.
- `/executions` must not select a business strategy or decide business success.
- `/evidence` must not mutate workflow/execution state.
- `/reporting` must never mutate authoritative domain state.
- `/operating-graph` is a derived coordination model over canonical authorities (read-only composition): it must never mutate authoritative domain state, must store only source references (canonical ids, kinds, versions and the scope chain — never shadowed authoritative shape), and must never become a second authority for any composed module.
- `/decisions` is the v1.5 append-oriented Decision Ledger authority (spec/architecture-v1.5.md §4; spec/change-request-005.md change #2). It consumes the listed public contracts READ-ONLY for write-time reference validation and canonical ownership resolution; `/policies` is the reserved direction for the MKT-045 consequential-action flow (deliberately unused by the ledger itself — recording a decision is unconditional, architecture-v1.5.md §7). The ledger never mutates historical execution, evidence, outcome or learning records (architecture-lock-v1.5.md rule #5).
- `/app-installs` is the v1.5 App installation authority (spec/mos-app-ecosystem-v1.5.md "Install and invoke", "Bounded app state", "Upgrade and rollback"; architecture-lock-v1.5.md rules #10/#11). It consumes the `/apps` registry public contract READ-ONLY (exact App Version resolution + the MKT-047 compatibility query — the registry is never mutated from here) and the `/policies` public contract for the fail-closed install/upgrade/rollback gate and the per-scope server-derived grant evaluations (every evaluation recorded by the engine); `/workspaces` and `/extensions` arrive through declared STRUCTURAL PORTS wired at the composition root (canonical workspace ownership; the workspace's authorized extension versions as the compatibility inputs). Installation is workspace-scoped and policy-gated; granted scopes are always SERVER-DERIVED (never caller-supplied); the install ledger is append-only with the single sanctioned selection-supersession transition — historical invocation records retain their original exact App Version identity (architecture-lock-v1.5.md rules #10/#11).
- `/profit-intelligence` is the v1.5 derived Profit Intelligence read model (spec/architecture-v1.5.md §5; architecture-lock-v1.5.md rule #6). It consumes the listed public contracts READ-ONLY through the /reporting live-aggregation precedent: every material figure is computed at read time with source references, the frozen calculation version and the explicit assumption set, and the module owns NO durable state (no profit_intelligence_* tables, no persisted projection). It can recommend but must never mutate contracts, billing or any financial/accounting authority — no mutation method over any authority may exist on its contract.
- `/ai-operator` is the v1.5 AI Operator attention-queue read model (spec/architecture-v1.5.md §7). It consumes the listed public contracts READ-ONLY through the /profit-intelligence derived-read-model precedent: attention items are live-derived at read time with a frozen category vocabulary and rank calculation version, source references to canonical record ids, structured rationale and the EXISTING consequential-action contract reference the item would flow through — the module owns NO durable state (no ai_operator_* tables, no persisted queue) and exposes ZERO mutation methods: it ranks action candidates only, never executes or creates them (consequential actions continue through the existing policy/approval contracts; scope-leakage and margin-pressure items consume the /profit-intelligence public figures and never recompute them).
- `/sales-continuity` is the v1.5 Sales-to-Delivery Continuity orchestrator (spec/architecture-v1.5.md §8). It consumes /decisions READ-ONLY (the proposal record + canonical ownership), orchestrates the /playbooks and /deployments EXISTING public creation/status commands (the only writes it can cause anywhere — never a second playbook/deployment store, never a write to their tables, never the MKT-040 validate/activate gate), resolves /clients + /workspaces ownership through its declared structural ports, and consumes the /evidence public contract's shared §21 material-key guard (READ-ONLY, the /decisions precedent) over the carried snapshot. Its own durable state is exactly the append-only continuity ledger (source references + version identity + carried snapshot + linkage) with forward-only completion; the carried identity is never rewritten — a new proposal version carries fresh identity.
- `/client-memory` is the v1.5 Client Operating Memory derived read model (spec/architecture-v1.5.md §6: a governed projection over canonical client, goal, playbook, deployment, evidence, experiment, outcome, decision and learning records; retrieval/index technology is non-authoritative). It consumes the listed public contracts READ-ONLY through the /reporting + /profit-intelligence live-derivation precedent: every memory view is composed at read time with canonical record citations only (never a shadowed authoritative shape) under the frozen, versioned projection vocabulary (cm-proj-v1) with its explicit selection rules, and the module owns NO durable state (no client_memory_* tables, no persisted projection, no retrieval index — nothing to rebuild). It must never expose a mutation method over ANY authority and may never become a second tenant or data authority.
- `/extensions` must not mutate `/workflows` except through an authorized workflow command/port.
- `/app-marketplace` is the v1.5 App marketplace, trust and certification surface (spec/mos-app-ecosystem-v1.5.md "Trust levels" + "Manifest" + "Economics"). It consumes the `/apps` registry public contract READ-ONLY for every catalog fact (the listing, version history and exact-version resolution — the marketplace NEVER becomes a second registry: it owns no app catalog table, only the append-only trust_events governance ledger and the app_reviews display-metadata records). Trust transitions are append-only events with operator provenance — never registry rewrites (the registry's certification column stays frozen at birth); trust is METADATA and a policy input that never grants authority by itself. The MKT-048 install gate consumes the marketplace-derived trust state through a STRUCTURAL PORT declared in `/app-installs`'s public entry and wired at the composition root (the `/policies`→`/credentials` port precedent — deliberately off-matrix, disclosed); reviews are display metadata no policy consumes. Marketplace attribution stays separate from the core financial authority (metering/attribution is later Work Item territory).
- `/app-metering` is the v1.5 App metering and commercial attribution authority (spec/mos-app-ecosystem-v1.5.md "Economics": "MOS may meter installations, invocation count, compute/runtime, data volume and premium capabilities. Marketplace attribution is separate from the core financial authority."). It consumes the `/apps` registry public contract READ-ONLY (exact App Version resolution for the ingestion manifest guards + the lineage listings for the publisher view), the `/app-installs` public contract READ-ONLY (the install-ledger selection rows and lifecycle event tail are the COLLECTION SOURCES; the current selections are the ingestion context and the invocation-attribution linkage inputs — the ledger is never written from here), the `/extensions` public contract READ-ONLY (the invocation ledger rows are the invocation COLLECTION SOURCE and the ingestion provenance validation), and `/workspaces` through a declared STRUCTURAL PORT (canonical workspace ownership, the /app-installs precedent). Its durable state is exactly the append-only meter event tail plus the rebuildable rollup projection (migration 044 — the at-most-once source fence, the payload-shape and unit CHECKs, the scope-chain and canonical-identity triggers, and append-only UPDATE/DELETE rejection); the collection, usage-observation ingestion and rollup recompute are MODULE-LEVEL operations for server-side callers (the operating-graph rebuild precedent) and the HTTP surface is the GET-only attribution family. The module exposes ZERO billing/charging mutation methods of any kind — no invoices, no payments, no balance mutations, no prices: meter records carry quantities, never money (architecture-lock v1.5 #6's financial-authority posture; the Economics separation).
- `/first-party-apps` is the v1.5 Incumbent Capability App Program composition home (spec/effective-backlog-v1.5.md MKT-051; spec/mos-app-ecosystem-v1.5.md "What Apps can do" / "Non-goals"; spec/architecture-v1.5.md §9). It consumes the `/apps` registry and the `/app-installs` ledger READ-ONLY (the pinned manifest of the workspace's CURRENT selection — exact App Version identity; the granted scopes that constrain presentation), and the incumbent authorities' public contracts READ-ONLY through the /reporting + /profit-intelligence live-aggregation precedent (with /workspaces for the workspace enumeration the /reporting contract's scope-as-data input requires): every pack surface model is composed at read time from the authorities' own views with canonical source references, never recomputed or shadowed. Pack code holds ZERO mutation verbs over ANY authority (the strict MKT-051 discipline): action menus DECLARE actions whose invocation contracts reference the EXISTING authority command routes, and every mutation an operator then makes flows through those routes under the platform's own authorization — never through pack logic. Bounded app state is IN-MEMORY, namespaced under the pack's own manifest-declared `app:<key>:<local>` namespaces (the migration-037 denylist respected), with explicit export/delete semantics and lineage to canonical record ids; the module owns NO durable state, takes NO migration and holds NO database dependency (packs are registry data + presentation code over existing authorities).
- `/growth-missions` is the v1.6 Growth Mission and Objective Model authority (spec/effective-backlog-v1.6.md MKT-053; spec/architecture-v1.6.md §1/§2/§3; spec/architecture-lock-v1.6.md rules 16/17/41). It consumes `/agencies` and `/goals` through declared STRUCTURAL PORTS, both READ-ONLY (the agency row for the fail-closed agency-scope resolution; canonical goal resolution for the mission→goal mapping — the Goal authority stays the sole measurable business-intent authority and goal progress is never re-stated, re-computed or owned here). Its durable state is exactly the agency-scoped mission records plus the append-only version tail (the declared objective is immutable — corrections are NEW version records), the append-only history tail (state transitions with actor + provenance + reason) and the goal-mapping rows (migration 045 — CHECK-fenced family/state vocabularies and append-only UPDATE/DELETE rejection triggers); it is a durable orchestration LAYER over existing Goals, never a replacement authority, and holds NO controller, scheduler or replanner — the Growth Operator is MKT-054 (architecture-lock-v1.6.md rules 16/17).
- `/social-accounts` is the v1.6 Social Account and OAuth Connection Model authority (spec/architecture-v1.6.md §4; spec/effective-backlog-v1.6.md MKT-055; architecture-lock-v1.6.md rules 18/19/28). It consumes the `/integrations` public contract READ-ONLY for the canonical integration connection reference + ownership chain (a social account attaches only through an EXISTING authorized — `connected` — integration; the connection, never this module, owns the client chain), the `/credentials` public contract as the vault authority for the grant's OWN least-privilege token references (kind `social_account_oauth` — created, resolved in-process after a fail-closed allow, and disabled through the vault; NEVER shared with product/source/store credentials, and NO token material ever enters the module's tables — the migration-046 grant rows carry the vault reference only), `/policies` for the fail-closed network/secrets gates in front of every provider-touching flow step (social-account.complete/refresh/revoke + social-account.credential), and `/workspaces` through a declared STRUCTURAL PORT for the optional workspace narrowing of the attachment. The OAuth grant lifecycle is append-oriented (pending → authorized → expired/revoked/refreshed/superseded; refresh and reauthorize append NEW grant records + history events — the recorded authorization facts are immutable), the EXACT granted scope list is recorded VERBATIM plus the platform-normalized capability tags (a faithful RECORD, never an assumption of parity), one connection binds one platform identity with idempotent reconnect and fail-closed conflicting-binding rejection, and disconnect/revocation — operator-initiated and externally-signalled — is terminal and fail-closed (every authorization-bearing read refuses; replaced grants' vault references are disabled — no zombie grants). The module defines the provider-neutral OAuth FLOW PORT but ships NO flow implementation: flows arrive as data (the adapter-registry precedent) with the MKT-056+ adapter deliveries; until then the production registry is EMPTY and flow steps are refused fail-closed (no platform-specific knowledge lives in the module — the platform identity is the adapter key carried as data, architecture-lock-v1.6 rule 18).
- `/product-intelligence` is the v1.6 Product Intelligence authority (spec/architecture-v1.6.md §8; spec/effective-backlog-v1.6.md MKT-069; spec/module-dependency-matrix-v1.6.md boundary rule 7: read-only source-code/product inspection unless a separately granted write capability). It consumes the `/integrations` public contract through a declared narrow READ-ONLY STRUCTURAL PORT (getConnection + executeRead ONLY — executeMutation is structurally absent from the port, so the module cannot express a mutation toward any external source; authorized repository/workspace/catalog/analytics inputs carry a canonical `connected` integration connection reference of the same agency, read through the frozen per-kind normalized operation labels), the `/ai-runtime` public contract through a model-identity port (the AI-assistance disclosure on derived records must cite a registry-resolvable model), and `/evidence` through the shared §21 material-key guard READ-ONLY (the `/sales-continuity` `/decisions` precedent — the ONE `/evidence` import lives in the module's store; source facts are retained in the module's OWN migration-048 tables, never a second evidence authority). ROW COMPLETION (the disclosed additive registration, now CLOSED): the frozen v1.6 matrix row for `/product-intelligence` lists `/research`, `/evidence`, `/integrations` and `/ai-runtime`; the MKT-069 delivery registered the then-satisfiable subset (evidence, integrations, ai-runtime) with `/research` disclosed as joining at MKT-062 time — the MKT-062 delivery (this session) delivers `/research`, so the row above now registers the FULL frozen direction set (research, evidence, integrations, ai-runtime). DISCLOSED: the `/product-intelligence` MODULE CODE of the MKT-069 delivery still imports nothing from `/research` (the direction was reserved for the mission-planner compositions of the later MKT-070+ Work Items, which attach research sessions/insights BY REFERENCE); the row completion registers the ALLOWED direction, it does not fabricate an unused consumption — the honest additive-registration pattern, closed exactly as the MKT-069 note disclosed it would be. The agency row is NOT resolvable from the module (`/agencies` is not a frozen allowance of this row): agency-scope authorization resolves at the route layer (the `/app-metering` precedent) with the migration-048 FK anchor as the backstop. The declared inputs ride IMMUTABLE version records (corrections are NEW versions, never in-place rewrites); every retained source fact carries FULL provenance (source reference, fetched-at, extractor identity, content hash, extraction notes — extracted observations, never conclusions); the derived model records of the §8 derivation set carry their evidence references (FK-anchored same-context source facts), their AI-assistance disclosure (model identity + call reference) and a SERVER-COMPUTED verification state (a record without backing evidence is unverified and can never be presented as established; a verification-state transition is a NEW superseding record — the `/evidence` single-supersession discipline); risk flags (compliance/privacy/toxicity/commercial/operational + severity) are append-only; and the model records are attachable BY REFERENCE from missions later (MKT-070) through the read surface only — NO mission-strategy logic lives in the module.
- `/notification-delivery` is the v1.6 Notification Delivery Plane authority (spec/effective-backlog-v1.6.md MKT-068; spec/architecture-v1.6.md §14; spec/module-dependency-matrix-v1.6.md boundary rule 9). It consumes the `/notifications` BOUNDARY identity READ-ONLY (the MKT-001 boundary-only stub — the delivery plane fills the delivery surface that boundary reserved, and the boundary itself keeps no business logic), the `/policies` public contract for the fail-closed per-channel gates (the notification-channel policy keys `notification.channel.<channel>` — a channel not sanctioned for the urgency/event type fails closed into an honest refused receipt, never a silent drop) and the `/credentials` vault READ-ONLY through the email adapter (provider credential by vault REFERENCE id, resolved in-process in the notification's authorized-execution scope — never a raw secret in the module). Its durable state is exactly the notification records (the §14 field set), the event-occurrence dedup fence (at-most-one notification per (source kind, source id, event type, occurrence key) — a replayed occurrence surfaces honest duplicate-skipped receipts on the original), the append-only delivery-attempt receipt tail (delivered/failed/refused/duplicate-skipped; retries are NEW rows) and the in-app read-state projection with its append-only read transition (migration 047 — CHECK-fenced vocabularies and append-only UPDATE/DELETE rejection triggers). Delivery is pluggable per channel through the platform-neutral DeliveryAdapter contract (in-app + email MVP; WhatsApp/Telegram/SMS/Signal are declared-but-unimplemented capability keys — registering one is refused fail-closed until their Work Items arrive). It delivers messages ONLY and never becomes canonical task/action state (boundary rule 9): the delivery status is the adapter-plane lifecycle (pending → dispatched), never a business/task state, and no done/acknowledged/acted-on verb or column exists anywhere. The disclosed off-matrix structural-port wiring: the email recipient resolution composes the `/agencies` membership and `/users` identity authorities at the composition root (recipients resolve from the notification's own scope chain — never a request field, never guessed).
- `/growth-operator` is the v1.6 Growth Operator authority (spec/effective-backlog-v1.6.md MKT-054; spec/architecture-v1.6.md §13; spec/architecture-lock-v1.6.md rule 17; spec/module-dependency-matrix-v1.6.md boundary rule 3: "Growth Operator can create/advance mission decisions but cannot own workflow/execution lifecycle"). It consumes `/growth-missions` through a declared structural port (the mission record + composed detail + the setGrowthMissionStatus command — MKT-053 stays the mission authority; the operator advances mission state conservatively: running ↔ active, paused ↔ paused, the honest terminal recording), `/goals` READ-ONLY through a structural port (the goal ownership resolution + live goal rows — the measurable anchor; goal progress is never re-stated or re-computed here), `/workflows` and `/executions` as THE DELEGATION AUTHORITIES (every physical workflow object created/transitioned ONLY through their existing public commands — createWorkflow/createWorkflowDefinition/setWorkflowDefinitionStatus/createWorkflowInstance/transitionWorkflowInstance and createExecution with the §8 logical idempotency key; the execution port deliberately exposes NO transition/mutation method so the operator structurally cannot own execution lifecycle — the runtime plane drives executions and the operator only READS them), `/experiments` (the bounded next experiment declared through the existing Experiment authority), `/evidence` (the operator's own delegated-outcome observations appended through the Evidence authority + the client evidence tail READ-ONLY), `/decisions` (the operator's strategic replan decisions recorded in the canonical Decision Ledger with §8 idempotency), `/learnings` READ-ONLY (the strategy-selection evidence references), and `/policies` as the fail-closed delegation gate (dimension 'tools', operation 'growth-operator.delegate' — an explicit DENY is the genuine human-declared governance boundary → blocked_pending_human_action; an UNDECIDED evaluation fails closed as a skip). DISCLOSED REGISTRATION SUBSET (the /product-intelligence MKT-069 precedent): the frozen v1.6 matrix row additionally lists `/platform-health`, which arrives with MKT-066 (a later Worker B Work Item, not merged at this delivery's base) — this row registers the currently-satisfiable subset and `/platform-health` joins at MKT-066 time (the seam is preserved as the typed GrowthOperatorPlatformHealthPort, no import, no wiring). `/playbooks` and `/deployments` are matrix-listed allowances the MVP controller loop deliberately does not yet exercise — the MKT-070/072 mission planners compose them; disclosed in the runbook. The disclosed off-matrix structural-port wiring: the pursuit-scope resolution composes `/workspaces` READ-ONLY at the composition root (the /notification-delivery MKT-068 off-matrix precedent — the canonical workspace→client→agency chain validates the pursuit scope; the workspace is never mutated). Its durable state is exactly the per-mission controller records (the frozen state vocabulary + the budget/quota policy with the ZERO-default human-amplification inputs), the bounded plan steps with their DETERMINISTIC idempotency keys (UNIQUE per mission — the no-double-dispatch fence), and the append-only decision + state-transition tails (migration 052 after Tech-Lead renumber — CHECK-fenced vocabularies, the frozen transition-pair trigger, and append-only UPDATE/DELETE rejection triggers). It is NEVER a second workflow or execution engine: no task/job pickup, no execution lifecycle mutation, no sandbox leasing, no dispatch/queue submit, no worker pool and no retry orchestration exist anywhere in the module (architecture-lock-v1.6.md rule 17; proven by the boundary tests).
- `/content-rights` is the v1.6 Content Rights and Provenance authority (spec/effective-backlog-v1.6.md MKT-063; spec/architecture-v1.6.md §9; spec/frozen-manifest-v1.6.json hardPublicationRules: rightsUncertaintyFailsClosed, sourceLineageRequired, destinationPolicyGateRequired; spec/module-dependency-matrix-v1.6.md boundary rule 4: "Content Rights can block publication but cannot silently approve unclear rights"). It consumes the `/evidence` public contract READ-ONLY for the canonical resolution of every evidence link (source provenance, licence evidence, permission evidence, clearance review evidence — FK-anchored same-Client links with the migration-023 same-client trigger backstops; the evidence authority stays sole, never re-stated here) and the `/policies` public contract for the fail-closed destination gate of EVERY publication-gate evaluation (the policy key `content.rights.publication.<platform>` on the network dimension — every decision recorded in the policy engine's own append-only ledger; only an explicit allow permits, deny and unknown both BLOCK). DISCLOSED REGISTRATION SUBSET (the `/product-intelligence` MKT-069 precedent): the frozen v1.6 matrix row for `/content-rights` lists `/evidence`, `/policies` and `/content-assets`; `/content-assets` joined the row at MKT-064 time (the delivery completed the mutual registration — the seam was the OPAQUE id-based reference the module already spoke: grammar-fenced asset refs + the typed ContentAssetReferencePort, which /content-assets now satisfies with its real version records), so the row above registers the full frozen direction set (evidence, policies, content-assets). Its durable state is exactly the asset-level rights records (the frozen cr-vocab-v1 state vocabulary owned/license/platform_permitted/cleared/review/blocked + the explicit `unknown` initial state, CHECK-fenced; the REQUIRED /evidence-anchored source provenance; licence evidence required for licence-basis states; the valid_until expiry horizon evaluated fail-closed at gate time; the unique one-record-per-(client, asset ref) fence), the append-only state-transition event tail (the CHECK-fenced frozen transition table — `cleared` reachable ONLY from `review` via human_clearance with the clearance row; history is never mutated in place), the human clearance records (actor identity + REQUIRED rationale + optional evidence reference — fair-use reasoning rides as review evidence, never an auto-clear), the destination-platform permission-scope rows (append-only; the newest row per platform is effective) and the immutable ingredient lineage links (the unique (client, composite, ingredient) fence; composites resolve as the CONJUNCTION of their ingredients — any unclear ingredient makes the composite review_required, any blocked/absent ingredient blocks it, and a composite without lineage links is blocked outright). The publication gate returns allow / review_required / blocked WITH reasons; an absent rights evaluation is BLOCKED, never allowed; unknown/review NEVER auto-approve (review_required is the one honest surface rights become a blocked_pending_human_action source for consumers); the gate holds NO publication authority — MKT-065 cross-platform distribution calls it before publishing, and no publish/dispatch verb exists anywhere in the module.
- `/content-assets` is the v1.6 Content Asset and Transformation Authority (spec/effective-backlog-v1.6.md MKT-064; spec/architecture-v1.6.md §10 "Transformation system"; spec/architecture-lock-v1.6.md rules 23/24; boundary rule 5: "Content Assets stores/derives artifact lineage but cannot become a rights authority"). It consumes the `/executions` public contract for the transformation EXECUTION flow (createExecution with the external-request task link `content-transformation:<id>` + transitionExecution for every lifecycle step — the module holds NO second execution engine, NO worker plane and NO runtime identity of its own; the referenced execution's lifecycle stays owned by /executions), the `/content-rights` public contract for the DERIVATION SEAM (recordLineageLink for composite → ingredient refs when the module derives assets — the 063 conjunction grammar; NEVER a rights-state write, NEVER a gate evaluation, NEVER a permission write: transforming an asset neither checks nor mutates rights — an asset whose ingredients are rights-blocked can still be TRANSFORMED here but can never pass the 063 publication gate), and the /object-storage direction of the frozen v1.6 row as the PLATFORM ObjectStore port (the MKT-001 contract — content-addressed keys; a platform import carries no matrix weight, exactly as the fs/memory/S3 adapters are wired at the composition root). DISCLOSED REGISTRATION SUBSET: the frozen v1.6 row lists `/executions`, `/object-storage` and `/content-rights`; the enforced row above registers the module-importable subset (executions, content-rights) — /object-storage is a platform capability direction, not a module, and the row's /evidence-anchored SOURCE PROVENANCE rides as the id-based reference seam (the FK + same-Client migration-053 triggers are the DB backstop; the route layer resolves evidence canonically first — the `/app-metering` precedent), because /evidence is not a frozen allowance of this row. Its durable state is exactly the logical asset identities with their immutable VERSIONED artifact records (explicit versions, no floating pointers — the (asset, version) fence; the opaque refs minted 'ca:' + canonical version id, globally unique, the target the /content-rights refs resolve to), the append-only lifecycle event tail, the append-only quality OBSERVATIONS (measurable facts from the closed metric vocabulary — duration, dimensions, bitrate, caption coverage, language, fps, sample rate, byte size; a fabricated 'score' is unrepresentable), the recorded TRANSFORMATIONS of the frozen family (crop/reframe/padding/compilation/clip/caption/voice/translation/format) with their IMMUTABLE ingredient links (explicit input versions resolved and frozen at request time) and their output version link set ONCE at completion, and the TransformationEngine capability port (engines registered as module DATA — first-party or Extension/App, EMPTY in production by default per the MKT-056 discipline; a kind with no registered engine fails closed). Derived assets are BORN with lineage: the output version is created 'derived' WITH its object and its transformation linkage in one transaction — an existing version can never be mutated into an output (the version discipline).
- `/experiment-analysis` is the v1.6 Experiment Analysis and Adaptive Allocation authority (spec/effective-backlog-v1.6.md MKT-067; spec/architecture-v1.6.md §12 "Experiment analysis"; the frozen v1.6 row verbatim names its allowed dependencies: /experiments, /metrics, /evidence and /learnings — the enforced row above registers exactly that set). It consumes the `/experiments` public contract READ-ONLY for the experiment identity/design anchor (resolveExperimentOwnership — the uniform-404 ownership chain; the module NEVER creates, mutates or concludes experiments: the /experiments authority stays sole for experiment identity, design and lifecycle, this is the analysis layer, never a second experiment engine), the `/metrics` public contract READ-ONLY for the observation ledger one analysis consumes (listMetricObservationsForClient — the bounded house read, filtered by the experiment's declared primary-metric identity, the reserved 'arm' split dimension and the declared observation window), the `/evidence` public contract READ-ONLY for the canonical same-Client resolution of every cited evidence link (getEvidence — uniform 404 on foreign identifiers; the DB same-Client trigger is the race backstop), and the `/learnings` public contract READ-ONLY for the confounder/outcome context of the client's learnings citing the analyzed experiment (listLearningsForClient). Its durable state is exactly the APPEND-ONLY ANALYSIS RECORDS (migration 054: the full §12 computed set as data — treatment/comparison effects, uncertainty, sample sizes, observation window, sequential-analysis state, confounders, limitations, practical effect threshold, recommended next allocation — each carrying the FULL input snapshot + the canonical deterministic input digest; UPDATE/DELETE rejected by DB triggers: a negative or inconclusive result is a first-class record, never discarded, never rewritten as success, never silently retried away) and the APPEND-ONLY ADAPTIVE-ALLOCATION RECOMMENDATIONS (migration 054: the bounded exploration/exploitation shares over declared strategy-variant arms with the EXPLORATION FLOOR AS RECORDED DATA (value + source, never a hardcoded magic number), the zero-capacity arms recorded with their exclusion reason — the human-growth invariant: a human-treatment arm with zero capacity never blocks, crashes or invalidates non-human allocation — and the full deterministic input snapshot + digest so re-running the allocator on the same inputs reproduces the same decision). The statistics core is fully deterministic (same inputs produce the same outputs; the row lists NO /ai-runtime dependency and none is used), and allocation results are RECOMMENDATIONS recorded as DATA toward the mission/operator layer — the module exposes zero methods that mutate experiment exposure, platform state or workflow inputs (no second execution engine). The /clients and /workspaces canonical ownership resolutions ride STRUCTURAL PORTS declared in its public contract (wired at the composition root — the metrics/experiments posture), so no forbidden module import exists.
- `/cross-platform-distribution` is the v1.6 Cross-Platform Distribution authority (spec/effective-backlog-v1.6.md MKT-065; spec/architecture-v1.6.md §5 "Cross-platform distribution"; spec/architecture-lock-v1.6.md rules 20 and 29; the frozen v1.6 row verbatim names its allowed dependencies: /growth-missions, /social-accounts, /content-assets, /content-rights, /integrations and /policies — the enforced row above registers exactly that set). It consumes the `/growth-missions` public contract READ-ONLY for the optional mission anchor (resolveGrowthMissionOwnership — the mission authority stays sole for mission identity/lifecycle; the module NEVER mutates a mission), the `/social-accounts` public contract for the destination account resolution (resolveAccountOwnership — uniform 404 on foreign bindings), the REAL per-platform capability matrix (resolveAccountCapabilityMatrix — the declared adapter capability subset, the usable authorization and the per-family scope satisfaction against the CURRENT grant's VERBATIM scopes; capability parity is NEVER assumed, lock rule 19) and THE 056 PUBLISH SUBMIT (submitPublish — the migration-050 idempotency ledger: the (account, idempotencyKey) at-most-once fence, provider refs, accepted/published/failed/restricted outcomes; the ONLY physical publish path — the module holds NO provider HTTP client, NO worker plane and NO second execution engine of its own), the `/content-assets` public contract READ-ONLY for the explicit versioned records behind every opaque 'ca:' ref (resolveAssetRef — the source asset, every declared transformation output and every destination's published ref resolve to EXPLICIT versions, never floating pointers), the `/content-rights` public contract for THE PUBLICATION GATE (evaluatePublicationGate — called BEFORE every publication attempt and honored FAIL-CLOSED: only `allow` permits an autonomous publish, review_required and blocked NEVER publish; the verdicts WITH reasons are recorded on the module's own append-only lineage tail, never re-evaluated — the module holds NO rights authority: the 063 gate stays sole, boundary rule 4), the `/integrations` public contract READ-ONLY for the adapter registry (listRegisteredAdapters — the 056 normalized adapter contract's platform pipe must be registered; a destination lacking its integration adapter is rejected fail-closed), and the `/policies` public contract for the DISPATCH GATE (evaluateAction — the network-dimension operation 'content.distribution.dispatch' with the destination platform as the resource; only an explicit allow permits, deny and unknown both BLOCK; the decisions ride the policy engine's own append-only ledger and their ids are recorded — the module holds NO policy authority). Its durable state is exactly the DISTRIBUTION PLAN records (migration 055: the §5 chain — source asset + transformation plan (versioned outputs) + optional mission anchor + destination variants, with the deterministic canonical input digest; the plan lifecycle planned → dispatching → dispatched with the idempotent re-dispatch convergence; identity immutable, no DELETE), the DESTINATION VARIANT rows (the per-destination target account/platform/format + the frozen destination-specific 056 publish request + the derived deterministic idempotency key cpd:<plan>:<destination>; the (plan, account, target format) unique fence; the frozen outcome vocabulary with the never-back-to-planned discipline), the per-destination PUBLICATION link records (the 056 attempt anchor + the submit-time state mirror + the provider refs + the duplicate flag; ONE append-only row per destination — the live publish truth stays the 056 ledger) and the fully APPEND-ONLY historical lineage tail (plan_created, dispatch_started, gate_evaluation, capability_resolution, policy_evaluation, publication_attempt, dispatch_completed, measurement_reference — the gapless per-plan sequence with full structured payloads and server-derived provenance; UPDATE/DELETE rejected by DB triggers: the lineage is historical, never rewritten). The fan-out is fail-closed PER DESTINATION (one destination's outcome never blocks the others); a destination lacking the required publish capability is rejected and recorded with reasons, never attempted blind; a 'submitted' 056 attempt stays the honest UNKNOWN (destination 'publishing' — pending reconciliation, never blindly replayed); measurement references are recorded as opaque DATA (the module never computes measurement — attribution is distinct from causal inference); plans, attempts and outcomes are DATA toward the mission/operator layer (§13 composes this module; it never composes the operator); the module is fully deterministic (the row lists NO /ai-runtime dependency and none is used).
- `/research` is the v1.6 Web Research authority (spec/effective-backlog-v1.6.md MKT-062; spec/architecture-v1.6.md §7 "Web Research"; the frozen v1.6 row verbatim names its allowed dependencies: /integrations, /evidence and /ai-runtime — the enforced row above registers exactly that set). It consumes the `/integrations` public contract through a declared narrow READ-ONLY STRUCTURAL PORT (getConnection + executeRead ONLY — executeMutation is structurally absent from the port, so NO mutation can be expressed toward any research source; authorized repository/workspace sources carry a canonical `connected` integration connection reference of the same agency, read through the frozen per-kind normalized operation labels `repository.read` / `workspace.read`, which await their adapters — a research pass over such a source today records the honest read_error outcome, never an invented read), the `/ai-runtime` public contract through a model-identity port (the AI-assistance disclosure on research insights must cite a registry-resolvable model identity + call reference), and `/evidence` through the shared §21 material-key guard READ-ONLY (the `/product-intelligence` `/decisions` precedent — the ONE `/evidence` import lives in the module's store; source facts are retained in the module's OWN migration-056 tables, never a second evidence authority). The public page fetch/extract capability is a REPLACEABLE port (the house port discipline): the GET-only ResearchPageReader contract (no method field — a mutation toward a public source is not expressible), the first-party HttpPageReader wired at the composition root, the disclosed in-repo test double in the test suites. Every material source fact retains FULL provenance (source reference, fetched-at, extractor identity, content hash, extraction notes — extracted observations, never conclusions); the derived research insight records carry their evidence references (FK-anchored same-session source facts), their AI-assistance disclosure (model identity + call reference) and a SERVER-COMPUTED verification state (a record without backing evidence is unverified and can never be presented as established; a verification-state transition is a NEW superseding record — the `/evidence` single-supersession discipline); research sessions, their declared queries/sources and their retained facts are immutable/append-only per the house lifecycle discipline (CHECK-fenced vocabularies, append-only UPDATE/DELETE rejection triggers). The read surface is what `/product-intelligence` consumes later and `/content-intelligence` consumes now, BY REFERENCE only.
- `/content-intelligence` is the v1.6 Content Intelligence authority (spec/effective-backlog-v1.6.md MKT-062; spec/architecture-v1.6.md §6 "Content Intelligence"; the frozen v1.6 row verbatim names its allowed dependencies: /evidence, /metrics, /experiments, /integrations and /research — the enforced row above registers exactly that set). It consumes the `/evidence` public contract as the SOLE evidence authority (the observation-ingestion pipeline appends every normalized platform observation as ONE canonical 'observation' evidence record through appendEvidence; every candidate/hypothesis evidence link is resolved canonically through getEvidence and FK-anchored same-Client with the migration-057 trigger backstops; the ONE §21 material-key guard import lives in the module's store), the `/metrics` public contract READ-ONLY (getMetricObservation — the observed-performance anchor validation of the candidates' optional FK-anchored metric observation references), the `/experiments` public contract READ-ONLY (getExperiment — the hypothesis experiment-reference validation; NO experiment is created or transitioned here: hypotheses are INPUTS to /experiments, never conclusions), the `/research` public contract READ-ONLY (resolveResearchInsightOwnership — the same-agency validation of the hypothesis research-insight citations; the AI-discipline disclosures ride the cited research records themselves, since this module's row lists NO /ai-runtime dependency and none is used), and the `/integrations` public contract through a declared narrow READ-ONLY STRUCTURAL PORT (getConnection + executeRead ONLY — the platform observations are read through the frozen per-observation-kind normalized operation labels: platform_analytics → 'runReport', the EXISTING generic-analytics adapter label; platform_content → 'content.list', which awaits its adapter and records the honest read-error outcome until one exists). Its durable state is exactly the migration-057 tables: the CLIENT-SCOPED append-only CANDIDATE records (the §6 observed-feature set as DATA — topic/entity, niche/sub-niche, content format, length, hook features, narrative structure, publishing time, observed performance, performance velocity, engagement, audience-fit signal, freshness, novelty, reuse/duplication risk — closed vocabularies where the house pattern uses them; a new observation is a NEW candidate, never an in-place rewrite), the FK-anchored same-Client evidence/metric/candidate link tables, the append-only HYPOTHESIS records with HONEST FRAMING (the §6 non-claim ships on every hypothesis view: observed competitor/platform performance does NOT by itself establish causality for the user's account; the single-supersession correction chain; the same-agency-fenced research citations; the READ-ONLY-validated optional experiment reference) and the honest observation-ingestion run records. The deterministic niche clustering ('ci-cluster-v1') and candidate ranking ('ci-rank-v1' — the disclosed frozen weight vector over the closed-vocab observed features) are reproducible PURE functions: same inputs produce the same outputs, and the ranked order is a recommendation recorded as DATA toward planners — never a mutation, never an outcome claim.
- No module imports another module's database repository implementation directly unless the matrix explicitly names that authority boundary and the imported symbol is part of its public contract.

## Composition root

Provider SDKs, concrete queue/storage clients, sandbox drivers, browser drivers and external integration adapters are wired at the composition root. Domain/application modules depend on provider-neutral contracts.
